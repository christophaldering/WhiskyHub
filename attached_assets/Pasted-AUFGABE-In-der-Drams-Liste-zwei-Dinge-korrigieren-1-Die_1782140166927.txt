AUFGABE: In der Drams-Liste zwei Dinge korrigieren:
(1) Die Dram-Liste (Karten) ist noch ~20px seitlich eingerückt gegenüber Titel/Kacheln — ein weiterer Wrapper mit horizontalem Padding.
(2) Im Status-Filter wird "Abgeschlossen 81" rechts abgeschnitten (die Zahl passt nicht). Beide Segment-Filter-Zeilen sollen etwas kompakter werden, damit lange Labels + Zahlen sicher passen.

Datei: client/src/labs/pages/LabsTasteDrams.tsx
KEINE andere Datei. KEIN Schema, KEIN drizzle, KEINE Migration. KEIN Commit.

═══════════════════════════════════════════
STEP 0 — DIAGNOSE (NUR LESEN, NICHTS ÄNDERN)
═══════════════════════════════════════════
Bestätige, dass es gibt:
(A) den Listen-Wrapper:  <div style={{ padding: "12px 20px 0" }}>
(B) im Origin-Segment einen Button mit  style={{ fontSize: 13, padding: "7px 8px" }}  und data-testid={`labs-origin-${fk}`}
(C) im Status-Segment einen Button mit  style={{ fontSize: 13, padding: "7px 8px" }}  und data-testid={`labs-status-${sk}`}
GIB MIR DIE BESTÄTIGUNG UND WARTE AUF MEIN "OK". Ändere noch nichts.

═══════════════════════════════════════════
EDIT 1 (A) — Listen-Wrapper bündig
═══════════════════════════════════════════
>>> ALT >>>
          <div style={{ padding: "12px 20px 0" }}>
            {isError ? (
<<< ENDE ALT <<<
>>> NEU >>>
          <div style={{ padding: "12px 0 0" }}>
            {isError ? (
<<< ENDE NEU <<<

═══════════════════════════════════════════
EDIT 2 (B) — Origin-Segment kompakter
═══════════════════════════════════════════
>>> ALT >>>
                  style={{ fontSize: 13, padding: "7px 8px" }}
                  data-testid={`labs-origin-${fk}`}>
                  {t(ORIGIN_I18N[fk])}
                  <span style={{ marginLeft: 6, fontSize: 11, opacity: 0.6, fontVariantNumeric: "tabular-nums" }}>{originCounts[fk]}</span>
<<< ENDE ALT <<<
>>> NEU >>>
                  style={{ fontSize: 12, padding: "7px 5px" }}
                  data-testid={`labs-origin-${fk}`}>
                  {t(ORIGIN_I18N[fk])}
                  <span style={{ marginLeft: 4, fontSize: 11, opacity: 0.6, fontVariantNumeric: "tabular-nums" }}>{originCounts[fk]}</span>
<<< ENDE NEU <<<

═══════════════════════════════════════════
EDIT 3 (C) — Status-Segment kompakter
═══════════════════════════════════════════
>>> ALT >>>
                  style={{ fontSize: 13, padding: "7px 8px" }}
                  data-testid={`labs-status-${sk}`}>
                  {t(STATUS_I18N[sk], STATUS_FALLBACK[sk])}
                  <span style={{ marginLeft: 6, fontSize: 11, opacity: 0.6, fontVariantNumeric: "tabular-nums" }}>{statusCounts[sk]}</span>
<<< ENDE ALT <<<
>>> NEU >>>
                  style={{ fontSize: 12, padding: "7px 5px" }}
                  data-testid={`labs-status-${sk}`}>
                  {t(STATUS_I18N[sk], STATUS_FALLBACK[sk])}
                  <span style={{ marginLeft: 4, fontSize: 11, opacity: 0.6, fontVariantNumeric: "tabular-nums" }}>{statusCounts[sk]}</span>
<<< ENDE NEU <<<

═══════════════════════════════════════════
VERIFIKATION
═══════════════════════════════════════════
- Die Datei kompiliert ohne neue TypeScript-Fehler.
- Die Dram-Karten sitzen jetzt linksbündig mit Titel/Kacheln (keine seitliche Einrückung mehr).
- Im Status-Filter ist "Abgeschlossen 81" vollständig sichtbar (nichts abgeschnitten).
- Beide Filter-Zeilen sehen einheitlich aus.

WICHTIG: NICHT committen. NICHT pushen. KEIN drizzle-kit, KEINE Migration, KEIN Schema-Edit. Nur speichern und mir melden — ich committe/republishe selbst.
