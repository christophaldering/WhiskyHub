HARD PROMPT — CaskSense: Cooper-Fass-Icon (CooperBarrel) einbauen

ZIEL
Coopers Zeichen wird ein gefügtes Fass mit innerem Schein (ruhend = klares
Linien-Symbol, glimmend = atmende Glut). Neue Komponente CooperBarrel, kleiner
CSS-Block, und CooperChip nutzt künftig das Fass statt der alten Glut-Spans.

══════════════════════════════════════════════════════════
SCHRITT 0 — DIAGNOSE (NUR LESEN, NICHTS ÄNDERN)
══════════════════════════════════════════════════════════
Bestätige mir mit Ausgaben, BEVOR du irgendetwas schreibst:
1. client/src/labs/components/rating/CooperBarrel.tsx existiert NICHT
   (ls; soll fehlen — es ist eine neue Datei).
2. Zeige den GESAMTEN aktuellen Inhalt von
   client/src/labs/components/rating/CooperChip.tsx.
3. In client/src/labs/labs-theme.css existiert dieser Block (Anker):
       @media (prefers-reduced-motion: reduce) {
         .cooper-glimmer-core, .cooper-glimmer-halo, .cooper-glimmer-ring { animation: none; }
         .cooper-glimmer-ring { display: none; }
       }
   Zeige ihn mit den ~3 Zeilen davor und danach.
4. Bestätige: CooperChip wird nur in
   client/src/labs/components/rating/RatingFlowV2.tsx verwendet (2×),
   beide Aufrufe OHNE glow-Prop. Diese Datei wird NICHT angefasst.

Gib dann einen kurzen Plan aus und WARTE auf mein ausdrückliches "OK".
Erst nach meinem OK weiter zu Schritt 1–3.

══════════════════════════════════════════════════════════
SCHRITT 1 — NEUE DATEI
══════════════════════════════════════════════════════════
Lege client/src/labs/components/rating/CooperBarrel.tsx an mit GENAU diesem Inhalt:

>>> NEUE DATEI: client/src/labs/components/rating/CooperBarrel.tsx >>>
// client/src/labs/components/rating/CooperBarrel.tsx
//
// Coopers Zeichen: ein gefügtes Fass (Cooper = der Küfer). Ruhend ein klares
// Linien-Symbol, glimmend mit innerem Schein, der atmet — die Glut erscheint
// erst, wenn Cooper aktiv ist. Reine Darstellung, theme-sicher über --labs-*.
// Styling/Animation in labs-theme.css (.cooper-barrel / .cooper-barrel-glow).

import { useId } from "react";

interface CooperBarrelProps {
  size?: number; // Kantenlänge in px (default 28 — Chip-Größe)
  glow?: boolean; // false = ruhend, true = glimmend (innerer Schein atmet)
  className?: string;
}

export default function CooperBarrel({ size = 28, glow = false, className }: CooperBarrelProps) {
  const gid = `cooper-glow-${useId().replace(/:/g, "")}`;

  const strong = { stroke: "var(--labs-amber)" } as const; // Reifen, Außenkanten, Deckel, Boden
  const stave = { stroke: "var(--labs-gold)" } as const; // innere Dauben

  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 72 72"
      fill="none"
      strokeLinecap="round"
      strokeLinejoin="round"
      className={`cooper-barrel${glow ? " is-glowing" : ""}${className ? ` ${className}` : ""}`}
      aria-hidden="true"
    >
      <defs>
        <radialGradient id={gid} cx="50%" cy="54%" r="60%">
          <stop offset="0%" style={{ stopColor: "#f6bc64", stopOpacity: 1 }} />
          <stop offset="48%" style={{ stopColor: "#e0922e", stopOpacity: 0.5 }} />
          <stop offset="100%" style={{ stopColor: "#e0922e", stopOpacity: 0 }} />
        </radialGradient>
      </defs>

      <ellipse className="cooper-barrel-glow" cx="36" cy="40" rx="13" ry="16" fill={`url(#${gid})`} />

      <g style={strong} strokeWidth="2.4">
        <path d="M24 20C14 31 14 47 24 58" />
        <path d="M48 20C58 31 58 47 48 58" />
      </g>

      <g style={stave} strokeWidth="2">
        <path d="M36 20V58" />
        <path d="M30 20.4C25.5 31 25.5 47 30 57.6" />
        <path d="M42 20.4C46.5 31 46.5 47 42 57.6" />
      </g>

      <path d="M24 58C29 60.5 43 60.5 48 58" style={strong} strokeWidth="2" />

      <ellipse cx="36" cy="20" rx="12" ry="3.8" style={{ ...strong, fill: "var(--labs-surface)" }} strokeWidth="2" />

      <g style={strong} strokeWidth="2.4">
        <path d="M16 28C28 32 44 32 56 28" />
        <path d="M14.5 39C28 44 44 44 57.5 39" />
        <path d="M16 50C28 54 44 54 56 50" />
      </g>
    </svg>
  );
}
<<< ENDE NEUE DATEI <<<

══════════════════════════════════════════════════════════
SCHRITT 2 — CSS in client/src/labs/labs-theme.css
══════════════════════════════════════════════════════════
Finde den Anker-Block und ersetze ihn 1:1 durch sich selbst PLUS den neuen Block.

>>> ALT (genau dieser Block) >>>
@media (prefers-reduced-motion: reduce) {
  .cooper-glimmer-core, .cooper-glimmer-halo, .cooper-glimmer-ring { animation: none; }
  .cooper-glimmer-ring { display: none; }
}
<<< ENDE ALT <<<

>>> NEU >>>
@media (prefers-reduced-motion: reduce) {
  .cooper-glimmer-core, .cooper-glimmer-halo, .cooper-glimmer-ring { animation: none; }
  .cooper-glimmer-ring { display: none; }
}

/* ── Cooper-Fass (Icon) ──────────────────────────────────────── */
.cooper-barrel-glow { opacity: 0; }
.cooper-barrel.is-glowing .cooper-barrel-glow {
  animation: cooper-glow-breathe 3.4s ease-in-out infinite;
}
@keyframes cooper-glow-breathe {
  0%, 100% { opacity: 0.42; }
  50%      { opacity: 1; }
}
@media (prefers-reduced-motion: reduce) {
  .cooper-barrel.is-glowing .cooper-barrel-glow { animation: none; opacity: 0.7; }
}
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 3 — CooperChip.tsx ERSETZEN (gesamter Dateiinhalt)
══════════════════════════════════════════════════════════
Ersetze den GESAMTEN Inhalt von
client/src/labs/components/rating/CooperChip.tsx durch:

>>> NEU (ganze Datei) >>>
import { useTranslation } from "react-i18next";
import CooperBarrel from "./CooperBarrel";

interface CooperChipProps {
  onClick: () => void;
  glow?: boolean; // optional: glimmt, wenn Cooper aktiv ist (default ruhend)
}

/**
 * Coopers Präsenz als kleiner, wiederkehrender Chip: das gefügte Fass mit
 * innerem Schein. Sitzt überall an derselben Stelle (Solo, Tasting, Dram-Detail)
 * und lädt zum Schärfen ein, ohne den Moment am Glas zu überlagern.
 * Icon: CooperBarrel. Chip-Styling in labs-theme.css (.cooper-chip).
 */
export default function CooperChip({ onClick, glow = false }: CooperChipProps) {
  const { t } = useTranslation();
  return (
    <button type="button" onClick={onClick} className="cooper-chip" data-testid="cooper-chip">
      <CooperBarrel size={28} glow={glow} />
      <span className="cooper-chip-label">{t("v2.cooperChip", "Cooper")}</span>
    </button>
  );
}
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
VERIFIKATION (lesend) — danach STOP
══════════════════════════════════════════════════════════
- `npx tsc --noEmit -p tsconfig.json` — es dürfen KEINE neuen Fehler in
  CooperBarrel.tsx / CooperChip.tsx auftauchen. (Bekannte umgebungsbedingte
  Meldungen zu node/vite-client/baseUrl ignorieren.)
- grep: client/src/labs/components/rating/CooperBarrel.tsx existiert.
- grep "CooperBarrel" in CooperChip.tsx → Import + Verwendung vorhanden.
- grep "cooper-barrel-glow" in labs-theme.css → vorhanden.
- RatingFlowV2.tsx wurde NICHT verändert (git diff --stat zeigt es nicht).

STRIKT VERBOTEN:
- KEIN git add / commit / push. NICHT committen — nur Dateien im Workspace ändern.
- KEIN drizzle-kit, keine Schema-Änderung, keine Migration.
- Keine weiteren Dateien anfassen (insb. RatingFlowV2.tsx, CooperGlimmer.tsx,
  useCooperVoice.ts bleiben unberührt).
- Nichts „aufräumen": die alten .cooper-glimmer*-CSS-Regeln stehen lassen.

Wenn etwas vom Step-0-Befund abweicht: NICHT raten — stoppen und mich fragen.
