# HARD PROMPT — CaskSense: Cooper-Einstieg Daten-Fundament (Etappe B1)

## Ziel
Eine kontogebundene Präferenz „Cooper-Einstieg" vorbereiten: eine neue additive Spalte `cooper_entry_mode` auf der participants-Tabelle und die passende Server-Whitelist im participant-PATCH. Werte: `auto` / `voice` / `type` / `null`. Noch keine UI — nur das Fundament.

## WICHTIG — Verhaltensregeln (für diese Etappe angepasst!)
- **SCHRITT 0 zuerst (NUR LESEN):** Beide Dateien öffnen, Anker bestätigen, kurze Bestätigung + Mini-Plan ausgeben. **Dann WARTE auf mein OK.**
- Erst nach meinem OK ausführen.
- **Die Schema-Datei DARF editiert werden** — es ist eine rein **additive** Spalte (kein Umbenennen, kein Löschen, keine Typänderung bestehender Spalten).
- **ABER: KEIN `drizzle-kit` ausführen** (kein `push`, kein `generate`, keine Migration). Das übernehme ich anschließend manuell.
- **KEIN commit. KEIN git.**
- Nur die zwei genannten Dateien anfassen.

---

## SCHRITT 0 — Diagnose (nur lesen, dann warten)

Datei A: `shared/schema.ts`
- Bestätige die Zeile (in der participants-Tabelle, umgeben von `preferredRatingScale`, `privacyConsentAt` usw.):
```
  preferredRatingMode: text("preferred_rating_mode"),
```

Datei B: `server/routes.ts`
- Bestätige im `app.patch("/api/participants/:id", ...)`-Handler den Block:
```
      if (req.body.preferredRatingMode !== undefined) {
        const m = req.body.preferredRatingMode;
        if (m === null) {
          updates.preferredRatingMode = null;
        } else if (["guided", "compact", "quick"].includes(String(m))) {
          updates.preferredRatingMode = String(m);
        } else {
          return res.status(400).json({ message: "Invalid rating mode. Must be guided, compact, quick or null." });
        }
      }
```

**Wenn beides passt: kurze Bestätigung + Plan ausgeben und auf mein OK warten.**

---

## SCHRITT 1 — Datei A: additive Spalte

Suche:
>>> ALT >>>
```
  preferredRatingMode: text("preferred_rating_mode"),
```
<<< ENDE ALT <<<

Ersetze durch:
>>> NEU >>>
```
  preferredRatingMode: text("preferred_rating_mode"),
  cooperEntryMode: text("cooper_entry_mode"),
```
<<< ENDE NEU <<<

---

## SCHRITT 2 — Datei B: Server-Whitelist erweitern

Suche:
>>> ALT >>>
```
      if (req.body.preferredRatingMode !== undefined) {
        const m = req.body.preferredRatingMode;
        if (m === null) {
          updates.preferredRatingMode = null;
        } else if (["guided", "compact", "quick"].includes(String(m))) {
          updates.preferredRatingMode = String(m);
        } else {
          return res.status(400).json({ message: "Invalid rating mode. Must be guided, compact, quick or null." });
        }
      }
```
<<< ENDE ALT <<<

Ersetze durch:
>>> NEU >>>
```
      if (req.body.preferredRatingMode !== undefined) {
        const m = req.body.preferredRatingMode;
        if (m === null) {
          updates.preferredRatingMode = null;
        } else if (["guided", "compact", "quick"].includes(String(m))) {
          updates.preferredRatingMode = String(m);
        } else {
          return res.status(400).json({ message: "Invalid rating mode. Must be guided, compact, quick or null." });
        }
      }
      if (req.body.cooperEntryMode !== undefined) {
        const c = req.body.cooperEntryMode;
        if (c === null) {
          updates.cooperEntryMode = null;
        } else if (["auto", "voice", "type"].includes(String(c))) {
          updates.cooperEntryMode = String(c);
        } else {
          return res.status(400).json({ message: "Invalid cooper entry mode. Must be auto, voice, type or null." });
        }
      }
```
<<< ENDE NEU <<<

---

## VERIFIKATION
- `shared/schema.ts`: enthält genau eine neue Zeile `cooperEntryMode: text("cooper_entry_mode"),` direkt unter `preferredRatingMode`.
- `server/routes.ts`: enthält genau einen neuen `if (req.body.cooperEntryMode !== undefined)`-Block direkt nach dem `preferredRatingMode`-Block.
- TypeScript bleibt sauber (keine neuen Fehler).
- **Nochmals: KEIN `drizzle-kit`, KEINE Migration, KEIN commit, KEIN git. Nur diese zwei Dateien editiert.**
