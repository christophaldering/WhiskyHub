ZIEL: Beim ALLERERSTEN Voice-Kontakt mit Cooper soll er sich kurz vorstellen (Methode + Einladung zu führen), gesprochen, EINMALIG. Danach nie wieder. Keine neue Pipeline, keine TTS, keine UI-Karte — nur ein flag-gesteuerter Instructions-Anhang. Die KI-Stimmen-Offenlegung existiert bereits (v2.voiceAiNote) und wird NICHT angefasst.

ROLLBACK-TAG zuerst setzen:
    git tag pre-cooper-voice-intro-2026-06-28

────────────────────────────────────────
SCHRITT 0 — DIAGNOSE VOR JEDER ÄNDERUNG (nur lesen, nichts schreiben)
Führe diese Greps aus und bestätige JEDE Zeile mit „gefunden, 1×". Bei Abweichung (0× oder >1×) SOFORT STOPPEN und mir den Ist-Stand melden — NICHT raten, NICHT anpassen.

  grep -n 'app.post("/api/voice-probe/token"' server/routes.ts
  grep -n 'const instructions = baseInstr + "\\n\\n" + continuerBlock' server/routes.ts
  grep -nc 'cooperVoiceIntroBlock' server/routes.ts        # erwartet: 0 (noch nicht vorhanden)
  grep -n 'body: JSON.stringify({ voice, mode, probe: opts?.probe === true })' client/src/labs/components/rating/useCooperVoice.ts
  grep -n 'if (st === "connected") { setStatus("connected"); setStatusText("verbunden' client/src/labs/components/rating/useCooperVoice.ts
  grep -nc 'labs_cooper_voice_intro_seen' client/src/labs/components/rating/useCooperVoice.ts   # erwartet: 0

Wenn der instructions-Anker in routes.ts NICHT exakt so steht (Replit kann vor GitHub sein), STOPP und zeig mir die echte Zeile.

────────────────────────────────────────
PLAN (erst zeigen, dann auf mein OK warten? NEIN — direkt ausführen, da reine additive SEARCH/REPLACE):
- 1 Block in server/routes.ts (Instructions-Anhang, server-seitig flag-gated)
- 2 Blöcke in client/src/labs/components/rating/useCooperVoice.ts (Flag im Token-Body + Flag erst bei echter Verbindung setzen)

────────────────────────────────────────
EDIT 1 — server/routes.ts
SUCHE (exakt, genau 1×):
      const instructions = baseInstr + "\n\n" + continuerBlock + " " + (continuerByLevel[cooperLevel] || continuerByLevel.adaptive) + "\n\n" + backdoorBlock + (knobV ? "\n\n" + knobV : "");

ERSETZE DURCH:
      const cooperVoiceIntroBlock = "EINMALIGE ERÖFFNUNG — nur dieses eine erste Mal: Bevor du zur offenen Einladung übergehst, stell dich in ein, zwei ruhigen Sätzen kurz vor und mach klar, wie ihr zusammenarbeitet — du hilfst dem Taster, seine eigenen Worte zu finden, du prägst nichts vor, und er bestimmt Tempo und Reihenfolge. Am Glas hältst du dich mit Wissen und Vergleichen zurück; seine Fragen beantwortest du gern danach. Lade ihn ein, sich Zeit zu lassen — es gibt kein richtig oder falsch. Geh dann sanft in die offene Einladung über, frei zu erzählen, was auffällt. Warm und knapp, kein Fachjargon, keine Aufzählung, nichts über diese Flasche. Dass deine Stimme KI ist, musst du nicht erwähnen — das steht bereits auf dem Bildschirm.";
      const wantsVoiceIntro = req.body?.intro === true && req.body?.probe !== true;
      const instructions = baseInstr + "\n\n" + continuerBlock + " " + (continuerByLevel[cooperLevel] || continuerByLevel.adaptive) + "\n\n" + backdoorBlock + (knobV ? "\n\n" + knobV : "") + (wantsVoiceIntro ? "\n\n" + cooperVoiceIntroBlock : "");

────────────────────────────────────────
EDIT 2 — client/src/labs/components/rating/useCooperVoice.ts
SUCHE (exakt, genau 1×):
      const tokenRes = await fetch("/api/voice-probe/token", { method: "POST", headers: { "Content-Type": "application/json", ...pidHeaders() }, body: JSON.stringify({ voice, mode, probe: opts?.probe === true }) });

ERSETZE DURCH:
      const wantIntro = opts?.probe !== true && (() => { try { return localStorage.getItem("labs_cooper_voice_intro_seen") !== "1"; } catch { return false; } })();
      const tokenRes = await fetch("/api/voice-probe/token", { method: "POST", headers: { "Content-Type": "application/json", ...pidHeaders() }, body: JSON.stringify({ voice, mode, probe: opts?.probe === true, intro: wantIntro }) });

────────────────────────────────────────
EDIT 3 — client/src/labs/components/rating/useCooperVoice.ts
SUCHE (exakt, genau 1×):
        if (st === "connected") { setStatus("connected"); setStatusText("verbunden — sprich jetzt"); setBusy(false); }

ERSETZE DURCH:
        if (st === "connected") { setStatus("connected"); setStatusText("verbunden — sprich jetzt"); setBusy(false); if (wantIntro) { try { localStorage.setItem("labs_cooper_voice_intro_seen", "1"); } catch { /* ignore */ } } }

────────────────────────────────────────
VERIFIKATION (nach den Edits, nur lesen)
  grep -nc 'cooperVoiceIntroBlock' server/routes.ts                                   # erwartet: 2
  grep -nc 'wantsVoiceIntro' server/routes.ts                                         # erwartet: 2
  grep -nc 'intro: wantIntro' client/src/labs/components/rating/useCooperVoice.ts     # erwartet: 1
  grep -nc 'labs_cooper_voice_intro_seen' client/src/labs/components/rating/useCooperVoice.ts  # erwartet: 2 (lesen + setzen)
  grep -nc 'wantIntro' client/src/labs/components/rating/useCooperVoice.ts            # erwartet: 3 (Definition + Body + connected)

Beide Dateien müssen ohne TypeScript-/Build-Fehler kompilieren. Wenn der Dev-Server bereits läuft, prüfe die Konsole auf Fehler in routes.ts und useCooperVoice.ts.

────────────────────────────────────────
STRIKTE VERBOTE (gelten für den GESAMTEN Lauf, auch in der Verifikation wiederholt):
  • KEIN git commit, KEIN git push, KEIN git add.
  • KEIN drizzle-kit (kein push, kein generate), KEINE Schema-Änderung, KEINE Migration.
  • KEINE weiteren Dateien anfassen außer den zwei genannten.
  • KEINE „Verbesserungen" über die drei SEARCH/REPLACE hinaus.
  • Bei jeder Anker-Abweichung: STOPP + Rückfrage, nicht improvisieren.
Wenn alles grün ist: nur melden „fertig, bereit zum Testen". Ich committe/pushe selbst.
