HARD PROMPT — Cooper-Skip-Schleife beheben (RatingFlowV2)

============================================================
STEP 0 — DIAGNOSE ZUERST, KEINE ÄNDERUNG
============================================================
1. Öffne client/src/labs/components/rating/RatingFlowV2.tsx
2. Bestätige durch Lesen: Das onSkip der ImpressionCapture-Instanz ruft aktuell
   setStep("mode"). Es gibt DREI setStep("cooper")-Einsprünge:
     (a) CooperChip im step==="mode"-Block
     (b) cooperAsRow-Button mit data-testid="rating-cooper-row"
     (c) CooperChip im Nicht-Row-Zweig des ratingTopBar
3. Bestätige: useCallback und useRef sind bereits aus "react" importiert (Zeile 1).
4. STOPPE und zeige mir deinen Plan, bevor du editierst. In Step 0 keine Dateiänderung.

============================================================
PLAN (erst nach meiner Bestätigung ausführen)
Genau diese 5 Edits in derselben Datei, sonst nichts.
============================================================

--- Edit 1 ---
Direkt NACH dieser Zeile:
  const [step, setStep] = useState<Step>(resolvedInitialMode ? "rating" : "mode");
einfügen:

  // Step the user was on before opening Cooper, so "Überspringen" returns there
  // (back to rating when a mode is already active) instead of always bouncing to the mode grid.
  const cooperReturnRef = useRef<Step>("mode");
  const enterCooper = useCallback((from: Step) => {
    cooperReturnRef.current = from;
    setStep("cooper");
  }, []);

--- Edit 2 ---
CooperChip im step==="mode"-Block:
  VORHER:  <CooperChip onClick={() => setStep("cooper")} />
  NACHHER: <CooperChip onClick={() => enterCooper("mode")} />

--- Edit 3 ---
onSkip der ImpressionCapture:
  VORHER:  onSkip={() => setStep("mode")}
  NACHHER: onSkip={() => setStep(cooperReturnRef.current)}

--- Edit 4 ---
cooperAsRow-Button (der mit data-testid="rating-cooper-row"):
  VORHER:  onClick={() => setStep("cooper")}
  NACHHER: onClick={() => enterCooper("rating")}

--- Edit 5 ---
CooperChip im Nicht-Row-Zweig (direkt vor {chipSlot}):
  VORHER:  <CooperChip onClick={() => setStep("cooper")} />
  NACHHER: <CooperChip onClick={() => enterCooper("rating")} />

============================================================
VERIFIKATION
============================================================
- grep -n 'setStep("cooper")' RatingFlowV2.tsx
    -> exakt EINE Trefferzeile (innerhalb enterCooper)
- grep -n 'enterCooper(' RatingFlowV2.tsx
    -> DREI Aufrufe ("mode", "rating", "rating")
- Nur EINE Datei geändert, ca. +11/-4 Zeilen.
- NICHT committen. Kein git add, kein git commit, kein Schema-/Migrations-Befehl,
  kein drizzle-kit. Änderungen NUR im Arbeitsbaum lassen, damit Christoph selbst
  prüfen und committen kann.

============================================================
ROLLBACK
============================================================
- Vorab Tag setzen:   git tag pre-cooper-skip-fix-2026-06-23
- Bei Bedarf zurück:  git reset --hard pre-cooper-skip-fix-2026-06-23
