HARD PROMPT — Such-Leerzustand: Erläuterung parallel zu Cooper

KONTEXT: In client/src/labs/components/LabsGlobalSearch.tsx zeigt der Such-Modus im Leerzustand nur „LETZTE SUCHEN". Wir ergänzen darüber eine kurze Erläuterung im selben Stil wie Coopers Leerzustand (Emblem-Kreis + Heading + eine Zeile), nur wenn keine Eingabe läuft (!hasQuery). Eine Datei, reines Frontend.

STEP 0 — DIAGNOSE (nichts ändern bis "go"):
Bestätige (read-only): Im Such-Zweig (nach `) : (`) gibt es den Container `<div … padding: "8px 16px 32px" …>` gefolgt von `{!hasQuery && recentSearches.length > 0 && (`. `Search` (lucide) ist importiert; `isDe` ist verfügbar. Melde kurz, dann WARTE auf "go".

STEP 1 — Erläuterung über den letzten Suchen einfügen
Ersetze
          padding: "8px 16px 32px",
        }}
      >
        {!hasQuery && recentSearches.length > 0 && (
durch
          padding: "8px 16px 32px",
        }}
      >
        {!hasQuery && (
          <div style={{ display: "flex", flexDirection: "column", alignItems: "center", textAlign: "center", gap: 12, padding: "32px 20px 24px", color: "var(--labs-text-secondary)" }}>
            <div style={{ width: 56, height: 56, borderRadius: "50%", background: "var(--labs-accent-muted)", display: "flex", alignItems: "center", justifyContent: "center" }}>
              <Search style={{ width: 24, height: 24, color: "var(--labs-accent)" }} />
            </div>
            <div style={{ fontFamily: "'EB Garamond', serif", fontSize: 22, color: "var(--labs-text)" }}>
              {isDe ? "Suche" : "Search"}
            </div>
            <div style={{ fontSize: 14, lineHeight: 1.5, maxWidth: 380 }}>
              {isDe
                ? "Schnell zum Ziel: Finde Whiskys, Seiten und Begriffe und spring direkt zum Eintrag. Für Fragen, Erklärungen und alles über deine eigenen Verkostungen wechsle zu Cooper."
                : "Straight to the point: find whiskies, pages and terms and jump right to the entry. For questions, explanations and anything about your own tastings, switch to Cooper."}
            </div>
          </div>
        )}
        {!hasQuery && recentSearches.length > 0 && (

VERIFIKATION: grep -c "Schnell zum Ziel" client/src/labs/components/LabsGlobalSearch.tsx → 1; Build grün; Such-Leerzustand zeigt Lupen-Emblem + „Suche" + Erläuterung über den letzten Suchen.
NICHT TUN: KEIN commit/push, KEIN drizzle/Schema, NUR diese Datei. Bei nicht-exaktem Match: STOPP und frag.
Rollback: git reset --hard <letzter Checkpoint>
