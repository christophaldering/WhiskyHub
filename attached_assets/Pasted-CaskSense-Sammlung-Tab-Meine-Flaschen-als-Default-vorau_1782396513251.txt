CaskSense — Sammlung-Tab: "Meine Flaschen" als Default vorauswählen, deplatzierte "ZULETZT BEWERTET"-Liste entfernen.
Datei: client/src/labs/pages/LabsTaste.tsx  (NUR diese Datei)

═══ SICHERHEIT ZUERST ═══
Setze zuerst einen Rollback-Tag:  git tag pre-sammlung-default-2026-06-25
VERBOTEN in diesem gesamten Auftrag: kein git commit, kein git push, kein drizzle-kit, keine Schema-/Migrations-Änderung. Nur Datei-Edits.

═══ SCHRITT 0 — DIAGNOSE (nur lesen, NICHTS ändern) ═══
Öffne client/src/labs/pages/LabsTaste.tsx und bestätige mir:
1) Es gibt den Block `if (activeTab === "collection") {` mit darin:
   - `const activeTile = COLLECTION_HUB_TILES.find((tile) => tile.testId === activeCollectionTile);`
   - `const activeTestId = activeTile?.testId;`
   - HubTileCollapsible mit `open={activeCollectionTile !== null}`
   - einem abschließenden Block `{!activeCollectionTile && ( ... <RecentRatedList ... /> ... )}`
2) `recentItems` (useMemo, ~Z.295) wird AUSSCHLIESSLICH von dieser RecentRatedList genutzt (sonst nirgends).
3) Import Z.13: `import { RecentRatedList, buildRecentRatedItems } from "@/labs/components/RecentRatedList";` — beide Symbole werden NUR im Collection-Block bzw. in recentItems verwendet.
4) `historyData` und `journalData` werden auch ANDERSWO genutzt (also NICHT anfassen).
Gib mir kurz Ist-Stand + Plan. Warte auf mein "go", bevor du irgendetwas änderst.

═══ SCHRITT 1 — ÄNDERN (erst nach meinem "go") ═══

1a) Ersetze im Collection-Block GENAU diesen Abschnitt:

      const activeTile = COLLECTION_HUB_TILES.find((tile) => tile.testId === activeCollectionTile);
      const activeTestId = activeTile?.testId;
      return (
        <div data-testid="meine-welt-inline-collection">
          <HubTileGrid
            tiles={COLLECTION_HUB_TILES}
            t={t}
            testIdPrefix="meine-welt"
            variant="single-row"
            role="nav"
            activeTestId={activeTestId}
            onTileClick={(tile) =>
              setActiveCollectionTile((prev) => (prev === tile.testId ? null : tile.testId))
            }
          />
          <HubTileCollapsible
            open={activeCollectionTile !== null}
            innerRef={inlineContentRef}
            className="labs-tastings-inline-content"
            style={{ marginTop: 16 }}
            testId={
              activeCollectionTile
                ? `meine-welt-collection-inline-${activeCollectionTile}`
                : undefined
            }
          >
            <EmbeddedMeineWeltProvider>
              {activeCollectionTile === "labs-link-collection-hub-bottles" && <LabsTasteCollection />}
              {activeCollectionTile === "labs-link-collection-hub-wishlist" && <LabsTasteWishlist />}
            </EmbeddedMeineWeltProvider>
          </HubTileCollapsible>
          {!activeCollectionTile && (
            <div style={{ marginTop: 24 }}>
              <RecentRatedList
                items={recentItems}
                limit={12}
                sectionTestId="meine-welt-recent-section"
                viewAllHref="/labs/taste/drams"
                headerVariant="meine-welt"
                hideViewAll
              />
            </div>
          )}
        </div>
      );

   DURCH GENAU DAS:

      const effectiveCollectionTile = activeCollectionTile ?? "labs-link-collection-hub-bottles";
      return (
        <div data-testid="meine-welt-inline-collection">
          <HubTileGrid
            tiles={COLLECTION_HUB_TILES}
            t={t}
            testIdPrefix="meine-welt"
            variant="single-row"
            role="nav"
            activeTestId={effectiveCollectionTile}
            onTileClick={(tile) =>
              setActiveCollectionTile((prev) => (prev === tile.testId ? null : tile.testId))
            }
          />
          <HubTileCollapsible
            open
            innerRef={inlineContentRef}
            className="labs-tastings-inline-content"
            style={{ marginTop: 16 }}
            testId={`meine-welt-collection-inline-${effectiveCollectionTile}`}
          >
            <EmbeddedMeineWeltProvider>
              {effectiveCollectionTile === "labs-link-collection-hub-bottles" && <LabsTasteCollection />}
              {effectiveCollectionTile === "labs-link-collection-hub-wishlist" && <LabsTasteWishlist />}
            </EmbeddedMeineWeltProvider>
          </HubTileCollapsible>
        </div>
      );

1b) Entferne den jetzt toten `recentItems`-useMemo (~Z.295) KOMPLETT:

  const recentItems = useMemo(
    () => buildRecentRatedItems(historyData, journalData, { participantId: currentParticipant?.id, preferredRatingScale: currentParticipant?.preferredRatingScale ?? 100 }),
    [historyData, journalData, currentParticipant?.id, currentParticipant?.preferredRatingScale],
  );

1c) Entferne die jetzt ungenutzte Import-Zeile (Z.13) KOMPLETT:

import { RecentRatedList, buildRecentRatedItems } from "@/labs/components/RecentRatedList";

   (NICHT die Datei RecentRatedList.tsx löschen — sie wird evtl. anderswo genutzt. Nur den Import hier.)

═══ SCHRITT 2 — VERIFIKATION (zeig mir die Ausgabe) ═══
cd ins Repo, dann:
- grep -n "RecentRatedList\|recentItems\|buildRecentRatedItems" client/src/labs/pages/LabsTaste.tsx   → MUSS 0 Treffer sein
- grep -n "effectiveCollectionTile" client/src/labs/pages/LabsTaste.tsx                                 → erwartet 4 Treffer (Definition + activeTestId + testId + 2 Content-Guards)
- grep -n "activeAITile !== null\|activeAnalyticsTile !== null\|activeTastingsLens" client/src/labs/pages/LabsTaste.tsx → KI/Analyse/Tastings unverändert vorhanden
- Projekt-Typecheck/Build laufen lassen (z.B. npm run check bzw. tsc --noEmit) → KEINE Fehler
ERINNERUNG: kein commit, kein push, kein drizzle, keine Schema-/Migrations-Änderung. Nur die obigen Datei-Edits.
