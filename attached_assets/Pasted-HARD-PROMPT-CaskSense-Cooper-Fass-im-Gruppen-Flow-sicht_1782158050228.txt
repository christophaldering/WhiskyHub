HARD PROMPT — CaskSense: Cooper-Fass im Gruppen-Flow sichtbar machen (3 Stellen)

ZIEL
Das Cooper-Fass-Icon (CooperBarrel) erscheint künftig überall, wo Cooper im
Live-/Gruppen-Flow auftritt — analog zur Solo-Einstiegskarte:
(1) in der freien "Mit Cooper schärfen"-Zeile (RatingFlowV2, rating-cooper-row),
(2) in der geführten Seitentür (LabsLive, live-cooper-sidedoor),
(3) neben dem Titel "Erster Eindruck" im Eindruck-Screen (ImpressionCapture) — dort
    GLIMMT das Fass (glow), sobald die Stimme verbunden ist ("Sprich frei"-Moment).
In den Zeilen: klein + einfarbig (mono, size 22) wie im Solo. Im Eindruck-Screen:
volles Fass (size 30), glow nur im connected Voice-Zustand.

══════════════════════════════════════════════════════════
SCHRITT 0 — DIAGNOSE (NUR LESEN, NICHTS ÄNDERN)
══════════════════════════════════════════════════════════
Bestätige mit Ausgaben, BEVOR du etwas änderst:
1. client/src/labs/components/rating/CooperBarrel.tsx existiert und exportiert
   default eine Komponente mit Props size/glow/mono.
2. RatingFlowV2.tsx: Zeile `import CooperChip from "./CooperChip";` vorhanden;
   der rating-cooper-row-Button enthält die Textzeile (ALT unten) WORTGLEICH.
3. LabsLive.tsx: Zeile `import ImpressionCapture from "@/labs/components/rating/ImpressionCapture";`
   vorhanden; die live-cooper-sidedoor-Textzeile (ALT unten) WORTGLEICH;
   `CooperBarrel` ist dort NOCH NICHT importiert.
4. ImpressionCapture.tsx: Zeile `import { useCooperVoice } from "./useCooperVoice";`
   vorhanden; der "Erster Eindruck"-Titel-Block (ALT unten) WORTGLEICH; `phase` und
   `voice` sind im Render-Scope (phase === "voice" / voice.status werden bereits genutzt).
Gib einen kurzen Plan aus und WARTE auf mein "OK". Erst nach dem OK Schritt 1–6.

══════════════════════════════════════════════════════════
SCHRITT 1 — RatingFlowV2: CooperBarrel importieren
══════════════════════════════════════════════════════════
>>> ALT >>>
import CooperChip from "./CooperChip";
<<< ENDE ALT <<<
>>> NEU >>>
import CooperChip from "./CooperChip";
import CooperBarrel from "./CooperBarrel";
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 2 — RatingFlowV2: Fass in die rating-cooper-row
══════════════════════════════════════════════════════════
>>> ALT >>>
        {t("v2.impressionSideDoor", "Mit Cooper schärfen")}
      </button>
    ) : (
<<< ENDE ALT <<<
>>> NEU >>>
        <span style={{ flexShrink: 0, display: "flex" }}><CooperBarrel size={22} mono /></span>
        <span>{t("v2.impressionSideDoor", "Mit Cooper schärfen")}</span>
      </button>
    ) : (
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 3 — LabsLive: CooperBarrel importieren
══════════════════════════════════════════════════════════
>>> ALT >>>
import ImpressionCapture from "@/labs/components/rating/ImpressionCapture";
<<< ENDE ALT <<<
>>> NEU >>>
import ImpressionCapture from "@/labs/components/rating/ImpressionCapture";
import CooperBarrel from "@/labs/components/rating/CooperBarrel";
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 4 — LabsLive: Fass in die geführte Seitentür
══════════════════════════════════════════════════════════
>>> ALT >>>
              {t("v2.impressionSideDoor", "Mit Cooper schärfen")}
            </button>
<<< ENDE ALT <<<
>>> NEU >>>
              <span style={{ flexShrink: 0, display: "flex" }}><CooperBarrel size={22} mono /></span>
              <span>{t("v2.impressionSideDoor", "Mit Cooper schärfen")}</span>
            </button>
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 5 — ImpressionCapture: CooperBarrel importieren
══════════════════════════════════════════════════════════
>>> ALT >>>
import { useCooperVoice } from "./useCooperVoice";
<<< ENDE ALT <<<
>>> NEU >>>
import { useCooperVoice } from "./useCooperVoice";
import CooperBarrel from "./CooperBarrel";
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 6 — ImpressionCapture: Fass neben "Erster Eindruck" (glimmt bei Voice)
══════════════════════════════════════════════════════════
>>> ALT >>>
      <div style={{ fontFamily: FONT.display, fontSize: 22, color: LABS_THEME.text, marginBottom: SP.xs }}>
        {t("v2.impressionTitle", "Erster Eindruck")}
      </div>
<<< ENDE ALT <<<
>>> NEU >>>
      <div style={{ display: "flex", alignItems: "center", gap: SP.sm, marginBottom: SP.xs }}>
        <span style={{ flexShrink: 0, display: "flex" }}><CooperBarrel size={30} glow={phase === "voice" && voice.status === "connected"} /></span>
        <div style={{ fontFamily: FONT.display, fontSize: 22, color: LABS_THEME.text }}>
          {t("v2.impressionTitle", "Erster Eindruck")}
        </div>
      </div>
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
VERIFIKATION (lesend) — danach STOP
══════════════════════════════════════════════════════════
- `npx tsc --noEmit -p tsconfig.json` — keine NEUEN Fehler in den drei Dateien.
  (Bekannte node/vite-client/baseUrl-Meldungen ignorieren.)
- grep "CooperBarrel" RatingFlowV2.tsx → Import + 1× in rating-cooper-row.
- grep "CooperBarrel" LabsLive.tsx → Import + 1× in Seitentür (2 Treffer).
- grep "CooperBarrel" ImpressionCapture.tsx → Import + 1× im Titel-Block.
- grep 'glow={phase === "voice"' ImpressionCapture.tsx → vorhanden.

STRIKT VERBOTEN:
- KEIN git add / commit / push. NICHT committen — nur Workspace-Dateien ändern.
- KEIN drizzle-kit, keine Schema-Änderung, keine Migration.
- Keine weiteren Stellen/Dateien anfassen. Den CooperChip (Pill-Variante in RatingFlowV2)
  und die Solo-Karte NICHT verändern.
Wenn etwas vom Step-0-Befund abweicht: NICHT raten — stoppen und mich fragen.
