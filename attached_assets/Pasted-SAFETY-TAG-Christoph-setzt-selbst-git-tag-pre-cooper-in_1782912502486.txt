SAFETY TAG (Christoph setzt selbst): git tag pre-cooper-intro-offer-2026-07-01

ZIEL:
Beim WIEDERHOLTEN Cooper-Voice-Aufruf (nicht beim allerersten) soll
Cooper in seiner Eröffnung zusätzlich anbieten, sich und sein Vorgehen
auf Wunsch noch einmal kurz vorzustellen — für Taster, die die erste
Vorstellung vergessen haben. Erstes Mal bleibt unverändert (volle
Vorstellung via cooperVoiceIntroBlock). Das Angebot ist ein Angebot,
kein Dirigieren; sagt der Taster ja, fasst Cooper Vorstellung +
Vorgehen FREI in eigenen Worten zusammen (nie wörtlich).

SCHRITT 0 — DIAGNOSE (nur lesen):
1. Zeige server/routes.ts Z.5679-5693: cooperVoiceIntroBlock (erstes
   Mal), cooperGreetingContract (jedes Mal), wantsVoiceIntro-Flag und
   die instructions-Zusammensetzung (Z.5693).
2. Bestätige die Mechanik: wantsVoiceIntro === true nur beim ersten
   Mal (Client-localStorage labs_cooper_voice_intro_seen); danach
   false → nur cooperGreetingContract greift.
3. Bestätige, dass langDirective/lang bereits im Scope ist (für DE/EN
   des neuen Angebots-Satzes).

SCHRITT 1 — PLAN (bestätigen):
A) Der cooperGreetingContract wird NICHT pauschal geändert — er gilt
   ja auch beim ersten Mal (wo das Angebot unnötig ist, weil gerade
   voll vorgestellt wurde). Stattdessen: ein neuer, separater Block
   cooperReintroOfferBlock, der NUR eingespeist wird, wenn
   wantsVoiceIntro === false (also beim wiederholten Mal).
   - Inhalt (DE): Ein Halbsatz in Coopers Eröffnung, sinngemäß —
     "...und wenn du magst, dass ich mich und mein Vorgehen noch
     einmal kurz vorstelle, sag einfach Bescheid — sonst fang an,
     wie es dir gefällt."
   - EN-Variante entsprechend.
   - WICHTIG (Nordstern): Kein Startschuss-Ton, kein "lass uns
     loslegen". Die Kontrolle bleibt beim Taster. Das Angebot ist
     beiläufig, nicht drängend.
   - Formulierung als Anweisung an Cooper (er variiert den Wortlaut
     ohnehin), nicht als wörtlich vorgeschriebener Satz.
B) Verhalten bei Annahme: Der Block instruiert Cooper, dass er —
   falls der Taster das Angebot annimmt ("ja", "stell dich vor",
   "was machst du nochmal") — sich und das Vorgehen FREI in eigenen
   Worten kurz zusammenfasst (nie wörtlich den Intro-Text abspulen):
   er hilft, eigene Worte zu finden; prägt nichts vor, bewertet/
   korrigiert nicht; Taster bestimmt Tempo und Reihenfolge; am Glas
   hält er sich mit Wissen/Vergleichen zurück. Knapp, gesprochen,
   nichts über diese Flasche. Danach zieht er sich zurück in die
   offene Einladung.
C) Einspeisung in Z.5693: cooperReintroOfferBlock nur anhängen wenn
   !wantsVoiceIntro. Reihenfolge sinnvoll relativ zum
   cooperGreetingContract wählen (das Angebot gehört in bzw. direkt
   nach die Eröffnung, vor cooperVoiceLifeBlock).
   - Erstes Mal (wantsVoiceIntro true): cooperVoiceIntroBlock wie
     bisher, KEIN Reintro-Offer.
   - Wiederholtes Mal (false): cooperGreetingContract +
     cooperReintroOfferBlock.
D) NUR der Voice-Schärfer (voice-probe/token). Text-Schärfer und
   Demo-Pfad unberührt.

SCHRITT 2 — AUSFÜHRUNG: nach "OK, umsetzen".

VALIDIERUNG (PASS/FAIL):
1. [ ] Erstes Mal (intro true): volle Vorstellung, KEIN Reintro-
       Angebot (Instruktions-String enthält cooperVoiceIntroBlock,
       nicht cooperReintroOfferBlock).
2. [ ] Wiederholtes Mal (intro false): cooperReintroOfferBlock im
       Instruktions-String, cooperVoiceIntroBlock NICHT.
3. [ ] Angebots-Text ohne Startschuss-/Dirigier-Ton (kein "lass uns
       los", kein Drängen); Kontrolle beim Taster.
4. [ ] DE + EN vorhanden (via langDirective/lang).
5. [ ] Text-Schärfer + Demo-Pfad unverändert.
6. [ ] npx tsc --noEmit: keine neuen Fehler über Baseline (509).

KEIN COMMIT/PUSH. Christoph committet selbst.