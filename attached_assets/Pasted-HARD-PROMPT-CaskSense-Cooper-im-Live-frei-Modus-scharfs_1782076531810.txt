HARD PROMPT — CaskSense: Cooper im Live-frei-Modus scharfschalten (Schritt 4, Teil 1b)

Drei Dateien: RatingFlowV2.tsx (Score-Konvertierung), LabsLive.tsx (Flag scharf), i18n.ts (Texte DE+EN).
Ziel: Im FREIEN Live-Modus erscheint der Cooper-Auftakt über dem Modus-Selektor (heute fehlt Cooper dort). Solo bleibt unverändert.

═══════════════════════════════
SCHRITT 0 — NUR DIAGNOSE, NICHTS ÄNDERN
═══════════════════════════════
Bestätige mir:
1. In RatingFlowV2.tsx gibt es im `if (step === "cooper")`-Block genau die Zeile `setLiveData(mapImpressionToRating(result, { prev: liveData ?? initialData }));`.
2. In LabsLive.tsx kommt die Zeile `whisky={{ name: displayName, region: currentWhisky?.region || undefined, cask: currentWhisky?.caskType || undefined, blind: isBlind }}` genau EINMAL vor (im freien Zweig, in der Nähe von `initialData={freeInitialData}`).
3. In i18n.ts steht `ratingLayoutStepwise: "Step by step",` (EN, ~Z.817) und `ratingLayoutStepwise: "Schritt f\u00fcr Schritt",` (DE, ~Z.10664).
WARTE auf mein „OK". Keine Änderung in Schritt 0.

═══════════════════════════════
E1 — RatingFlowV2.tsx (Score-Konvertierung aus scale)
═══════════════════════════════
>>> ALT >>>
        onApply={(result) => {
          setLiveData(mapImpressionToRating(result, { prev: liveData ?? initialData }));
          setMode("compact");
          setStep("rating");
        }}
<<< ENDE ALT <<<
>>> NEU >>>
        onApply={(result) => {
          const inv = scale ? 1 / scale.step : 1;
          const convertScore = scale && scale.max !== 100
            ? (v: number) => Math.round((v / 100) * scale.max * inv) / inv
            : undefined;
          const fallback = !scale || scale.max === 100 ? 75 : Math.round((scale.max * 0.75) / scale.step) * scale.step;
          setLiveData(mapImpressionToRating(result, { prev: liveData ?? initialData, fallback, convertScore }));
          setMode("compact");
          setStep("rating");
        }}
<<< ENDE NEU <<<

═══════════════════════════════
E2 — LabsLive.tsx (Flag scharf im freien Zweig)
═══════════════════════════════
>>> ALT >>>
                            whisky={{ name: displayName, region: currentWhisky?.region || undefined, cask: currentWhisky?.caskType || undefined, blind: isBlind }}
<<< ENDE ALT <<<
>>> NEU >>>
                            whisky={{ name: displayName, region: currentWhisky?.region || undefined, cask: currentWhisky?.caskType || undefined, blind: isBlind }}
                            enableCooperIntro={true}
<<< ENDE NEU <<<

═══════════════════════════════
E3 — i18n.ts EN
═══════════════════════════════
>>> ALT >>>
        ratingLayoutStepwise: "Step by step",
<<< ENDE ALT <<<
>>> NEU >>>
        ratingLayoutStepwise: "Step by step",
        cooperIntroTitle: "Talk with Cooper",
        cooperIntroDesc: "Let a conversation guide you through your first impression \u2014 Cooper puts it into words afterwards.",
<<< ENDE NEU <<<

═══════════════════════════════
E4 — i18n.ts DE
═══════════════════════════════
>>> ALT >>>
        ratingLayoutStepwise: "Schritt f\u00fcr Schritt",
<<< ENDE ALT <<<
>>> NEU >>>
        ratingLayoutStepwise: "Schritt f\u00fcr Schritt",
        cooperIntroTitle: "Sprich mit Cooper",
        cooperIntroDesc: "Lass dich im Gespr\u00e4ch durch den ersten Eindruck f\u00fchren \u2014 Cooper fasst ihn danach in Worte.",
<<< ENDE NEU <<<

═══════════════════════════════
VERIFIKATION
═══════════════════════════════
1. RatingFlowV2.tsx: der cooper-onApply leitet jetzt convertScore + fallback aus scale ab.
2. LabsLive.tsx: der freie RatingFlowV2 (bei freeInitialData) hat enableCooperIntro={true}; der geführte Zweig (mit liveCooperOpenId) ist UNVERÄNDERT.
3. i18n.ts: cooperIntroTitle + cooperIntroDesc in EN und DE.
4. Build/HMR ohne Fehler.
5. AM GLAS testen: In einem FREIEN Live-Tasting erscheint über dem Modus-Selektor der goldene Block „Sprich mit Cooper". Tippen → Cooper-Dialog → danach landet man in der In-Depth-Bewertung mit Coopers Vorbefüllung. In einem Tasting mit Nicht-100er-Skala stimmen die übernommenen Scores.

WICHTIG: KEIN git commit, KEIN git push, KEINE Migration, KEIN drizzle-kit. Nur diese drei Dateien. Republish/Push mache ICH selbst. Wiederhole: NICHT committen.
