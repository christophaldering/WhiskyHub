AUFGABE: Tasting-Cover oberhalb der Dram-Liste in der Live-Ansicht zeigen — aber nur, wenn das Tasting nicht blind ist (oder das Cover bereits enthüllt wurde).
NUR diese eine Datei: client/src/labs/pages/LabsLive.tsx
KEINE anderen Dateien. KEIN Schema, KEIN drizzle, KEINE Migration. KEIN Commit.

═══════════════════════════════════════════
STEP 0 — DIAGNOSE (NUR LESEN, NICHTS ÄNDERN)
═══════════════════════════════════════════
Öffne client/src/labs/pages/LabsLive.tsx und bestätige:
  - es gibt die Import-Zeile:  import AuthGateMessage from "@/labs/components/AuthGateMessage";
  - es gibt den Block mit data-testid="calibration-overview" (die "My Scores Overview"-Karte)
  - CoverImage16x9 wird noch NICHT importiert
GIB MIR DIE BESTÄTIGUNG UND WARTE AUF MEIN "OK". Ändere noch nichts.

═══════════════════════════════════════════
EDIT 1 — CoverImage16x9 importieren
═══════════════════════════════════════════
>>> ALT >>>
import AuthGateMessage from "@/labs/components/AuthGateMessage";
<<< ENDE ALT <<<
>>> NEU >>>
import AuthGateMessage from "@/labs/components/AuthGateMessage";
import CoverImage16x9 from "@/labs/components/CoverImage16x9";
<<< ENDE NEU <<<

═══════════════════════════════════════════
EDIT 2 — Cover-Banner oberhalb der Score-Overview-Liste
HINWEIS: direkt VOR der "calibration-overview"-Karte einfügen.
═══════════════════════════════════════════
>>> ALT >>>
          )}

          {whiskies && whiskies.length > 1 && (
            <div className="labs-card-elevated mt-4 labs-fade-in labs-stagger-5" data-testid="calibration-overview">
<<< ENDE ALT <<<
>>> NEU >>>
          )}

          {tasting?.coverImageUrl && (!tasting?.blindMode || tasting?.coverImageRevealed) && (
            <div className="mb-4" data-testid="live-tasting-cover">
              <CoverImage16x9 src={tasting.coverImageUrl} rounded={16} testId="live-tasting-cover-img" />
            </div>
          )}

          {whiskies && whiskies.length > 1 && (
            <div className="labs-card-elevated mt-4 labs-fade-in labs-stagger-5" data-testid="calibration-overview">
<<< ENDE NEU <<<

═══════════════════════════════════════════
VERIFIKATION
═══════════════════════════════════════════
- Datei kompiliert ohne neue TypeScript-Fehler.
- Bei einem NICHT-blinden Tasting mit Cover erscheint das Cover-Bild (16:9, abgerundet) direkt über der "My Scores Overview"-Liste.
- Bei einem Blind-Tasting bleibt das Cover verborgen, bis es enthüllt ist (coverImageRevealed).
- Ohne Cover (coverImageUrl leer) ändert sich nichts.

WICHTIG: NICHT committen. NICHT pushen. KEIN drizzle-kit, KEINE Migration, KEIN Schema-Edit. Nur speichern und mir melden — ich committe/republishe selbst.
