═══════════════════════════════════════════════════════════════════════════════
CASKSENSE — HARD PROMPT P2-A
SPEICHER-TERMINOLOGIE: "DRAFT" + "KONFIRMIEREN" EINHEITLICH
(Befunde #1, #11, #13, #14, #17)
═══════════════════════════════════════════════════════════════════════════════

KONTEXT: In der App gibt es aktuell 6+ verschiedene Formulierungen für
"Speichern". Diese werden auf 2 klare Begriffe vereinheitlicht:
  - "Draft" (sprachübergreifend, für unvollständige Bewertungen)
  - "Konfirmieren" / "Confirm" (für abgeschlossene Bewertungen)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 0 — PFLICHT-PRÜFUNG VOR JEDER ÄNDERUNG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Suche in client/src/labs/ und client/src/lib/i18n.ts nach allen Vorkommen
dieser Strings (exakt so, mit Anführungszeichen suchen):

  "Save progress"
  "save progress"
  "Nose save"
  "Palate save"
  "Finish save"
  "Zwischenstand sichern"
  "Als Entwurf speichern"
  "Entwurf speichern"
  "Entwürfe"
  "Entwurf"
  "speichern" (kleingeschrieben als Button-Text)
  "Ich bin fertig"
  "Als Entwurf"

Notiere für jeden gefundenen String:
  - Dateiname
  - Zeile
  - Kontext (Button-Label? i18n-Key? Hardcoded?)

Berichte die vollständige Liste BEVOR du etwas änderst.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 1 — NUR i18n.ts: Keys ersetzen / ergänzen
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

NUR in client/src/lib/i18n.ts.

Suche die bestehenden Keys für die betroffenen Strings und ersetze ihre
Werte (NICHT neue Blöcke ans Ende hängen — nur bestehende Werte ändern):

EN-Block (Werte ersetzen):
  saveProgress → "Save as Draft"
  noseSave (o.ä.) → "Save Nose → Palate"   [falls Key existiert]
  palateSave → "Save Palate → Finish"       [falls Key existiert]
  finishSave → "Save Finish → Overall"      [falls Key existiert]
  finalizeRating → "Confirm →"
  saveDraft → "Save as Draft"
  drafts (Tab-Label) → "Drafts"
  draft (Badge) → "Draft"
  iAmDone → "Confirm"
  addMoreDetails → "Rate in full →"

DE-Block (Werte ersetzen):
  saveProgress → "Als Draft sichern"
  noseSave → "Nase sichern → Gaumen"        [falls Key existiert]
  palateSave → "Gaumen sichern → Abgang"    [falls Key existiert]
  finishSave → "Abgang sichern → Gesamt"    [falls Key existiert]
  finalizeRating → "Konfirmieren →"
  saveDraft → "Als Draft sichern"
  drafts → "Drafts"
  draft → "Draft"
  iAmDone → "Konfirmieren"
  addMoreDetails → "Vollständig bewerten →"

WICHTIG: Nur Keys ändern die tatsächlich in deiner Prüfung (Schritt 0)
gefunden wurden. Keine Keys erfinden die nicht existieren.
WICHTIG: Niemals doppelte Blöcke erstellen. Nur im Original-Block ändern.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 2 — NUR HARDCODED STRINGS ERSETZEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Für alle hardcodierten Strings die du in Schritt 0 gefunden hast
(also Strings die NICHT über t() aufgerufen werden):

Ersetze jeden hardcodierten String durch den entsprechenden t()-Aufruf.
Beispiel:
  ALT: <button>"Save progress"</button>
  NEU: <button>{t("saveProgress")}</button>

WICHTIG: Nur wenn der String hardcodiert ist. Wenn er bereits t() verwendet,
nur den Key-Wert in i18n.ts anpassen (bereits in Schritt 1 erledigt).

NICHT anfassen: Button-Funktionalität, onClick-Handler, Styles, Layout.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 3 — FLOATING "SAVE PROGRESS" BUTTON ENTFERNEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

In den Rating-Seiten (Guided und Compact) gibt es einen floating
"Save progress" / "Zwischenstand sichern" Button der ZUSÄTZLICH zum
Haupt-CTA erscheint.

Finde diesen Button (suche nach dem Text aus Schritt 0).

Entferne NUR diesen floating Button — nicht den Haupt-CTA.

NICHT anfassen: den Haupt-CTA ("Nose save → Palate" etc.), die Speicher-
Logik dahinter, andere Buttons.

Falls der floating Button eine wichtige Speicher-Funktion hat die der
Haupt-CTA nicht abdeckt → nicht entfernen, stattdessen melden.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 4 — QUICK RATE ABSCHLUSS: OPTIONEN VEREINFACHEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Suche die Komponente die nach Quick Rate den Abschluss-Screen zeigt
("Schöner Dram! / Was möchtest du als nächstes tun?").

Ändere NUR die Button-Labels (nicht die Funktionalität):
  "Mehr Details hinzufügen" → t("addMoreDetails") = "Vollständig bewerten →"
  "Ich bin fertig" → t("iAmDone") = "Konfirmieren"
  "Als Entwurf speichern" → ENTFERNEN (Autosave hat bereits gesichert)

Suche das Emoji (🥂 oder ähnlich) in dieser Komponente:
  → Ersetze es durch ein einfaches SVG-Icon (Glas-Icon) oder entferne es.
  → Kein Emoji gemäß Design-Regeln.

NICHT anfassen: die onClick-Handler der Buttons, das Card-Layout,
den Score-Display.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SICHERHEITSREGELN — NIEMALS ANFASSEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✗ server/ — keine einzige Datei
✗ shared/schema.ts
✗ package.json / package-lock.json
✗ .env / capacitor.config.ts
✗ Speicher-Logik / API-Calls / Mutations
✗ Button onClick-Handler
✗ Layouts, Styles, CSS
✗ Alle Seiten die nicht explizit genannt sind

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFIKATION (nach Abschluss berichten)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ DE-Modus: Hauptbutton in Guided = "Nase sichern → Gaumen" (o.ä.)
□ EN-Modus: Hauptbutton in Guided = "Save Nose → Palate" (o.ä.)
□ DE-Modus: Finaler Button = "Konfirmieren →"
□ EN-Modus: Finaler Button = "Confirm →"
□ Tab-Labels: "Drafts" (nicht "Entwürfe") in DE und EN
□ Draft-Badge: "Draft" (nicht "Entwurf") in DE und EN
□ Floating "Save progress" / "Zwischenstand sichern" Button weg
□ Quick Rate Abschluss: 2 Buttons (nicht 3), kein Emoji
□ Geänderte Dateien: [Namen eintragen]
□ tsc --noEmit fehlerfrei