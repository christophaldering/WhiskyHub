HARD PROMPT — CaskSense: Gruppe 2a — Bewertungsform in den Tasting-Kopf (freier Live-Modus)

ZIEL
Im freien Live-Modus wird die Bewertungsform nicht mehr aus einem eigenen lokalen
State (freeRatingMode, Default "tisch") gezogen, sondern aus der schon vorhandenen
übergeordneten Quelle Session ?? Profil ?? "tisch". Die Form-Wahl (RatingModeChip)
sitzt künftig EINMAL oben im "My Scores Overview"-Kopf. Nur LabsLive.tsx wird
berührt. (Der Modus-Chip am Glas bleibt in diesem Schritt noch bestehen — er kommt
erst in 2b weg; bis dahin gibt es kurz zwei Chips, beide steuern dieselbe Quelle.)

══════════════════════════════════════════════════════════
SCHRITT 0 — DIAGNOSE (NUR LESEN, NICHTS ÄNDERN)
══════════════════════════════════════════════════════════
Bestätige mit Ausgaben, BEVOR du etwas änderst:
1. `freeRatingMode` / `setFreeRatingMode` kommen in LabsLive.tsx NUR an drei Stellen
   vor: die useState-Zeile, `preferredMode={freeRatingMode}`, und
   `if (draft.mode) setFreeRatingMode(draft.mode);`. (grep zeigen.)
2. `sessionRatingMode`, `setSessionRatingMode`, `preferredRatingModeFromProfile`,
   `handleSetPreferredMode` sind definiert.
3. KRITISCH (Scope): Die useState-Zeile von freeRatingMode, der Block mit
   `data-testid="calibration-header"` und die Zeile `({myAllRatings.length}/...rated)`
   liegen in DERSELBEN Komponentenfunktion, und in dieser Funktion sind `t`,
   `setSessionRatingMode`, `handleSetPreferredMode` erreichbar. Zeige dazu, in welcher
   Funktion diese Zeilen stehen (die `function`/`const … = (` davor).
4. `RatingModeChip` ist noch NICHT importiert.
Gib einen kurzen Plan aus und WARTE auf mein "OK". Erst danach Schritt 1–4.
Falls Punkt 3 (Scope) nicht zutrifft: NICHT bauen, mir den Befund melden.

══════════════════════════════════════════════════════════
SCHRITT 1 — Import RatingModeChip
══════════════════════════════════════════════════════════
>>> ALT >>>
import RatingFlowV2 from "@/labs/components/rating/RatingFlowV2";
<<< ENDE ALT <<<
>>> NEU >>>
import RatingFlowV2 from "@/labs/components/rating/RatingFlowV2";
import RatingModeChip from "@/labs/components/rating/RatingModeChip";
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 2 — freeRatingMode-State durch abgeleitete Quelle ersetzen
══════════════════════════════════════════════════════════
>>> ALT >>>
  const [freeRatingMode, setFreeRatingMode] = useState<"guided" | "compact" | "quick" | "tisch" | null>("tisch");
<<< ENDE ALT <<<
>>> NEU >>>
  // Bewertungsform im freien Modus: eine übergeordnete Quelle (Abend ?? Profil ?? Tisch).
  // Gesetzt wird sie im Tasting-Kopf (RatingModeChip) bzw. — bis 2b — noch am Glas.
  const effectiveFreeMode = sessionRatingMode ?? preferredRatingModeFromProfile ?? "tisch";
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 3 — RatingFlowV2 (freier Modus): Quelle + onChange umhängen
══════════════════════════════════════════════════════════
3a)
>>> ALT >>>
                            preferredMode={freeRatingMode}
<<< ENDE ALT <<<
>>> NEU >>>
                            preferredMode={effectiveFreeMode}
<<< ENDE NEU <<<

3b)
>>> ALT >>>
                              if (draft.mode) setFreeRatingMode(draft.mode);
<<< ENDE ALT <<<
>>> NEU >>>
                              if (draft.mode) setSessionRatingMode(draft.mode);
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
SCHRITT 4 — Form-Chip in den "My Scores Overview"-Kopf
══════════════════════════════════════════════════════════
Füge den Chip rechts in den calibration-header ein (zwischen dem Ende des
Titel-Blocks und dem schließenden div des Headers). Einrückung beibehalten.
>>> ALT >>>
                  <span className="text-[11px]" style={{ color: "var(--labs-text-muted)" }}>
                    ({myAllRatings.length}/{whiskies.length} rated)
                  </span>
                </div>
              </div>
<<< ENDE ALT <<<
>>> NEU >>>
                  <span className="text-[11px]" style={{ color: "var(--labs-text-muted)" }}>
                    ({myAllRatings.length}/{whiskies.length} rated)
                  </span>
                </div>
                <div style={{ marginLeft: "auto" }}>
                  <RatingModeChip
                    mode={effectiveFreeMode}
                    showTisch
                    labels={{
                      current: t("v2.ratingModeChipCurrent", "Modus"),
                      title: t("v2.ratingModeChipTitle", "Bewertungsform wechseln"),
                      guided: t("v2.ratingGuided", "Gefuehrt"),
                      compact: t("v2.ratingCompact", "Kompakt"),
                      quick: t("v2.ratingQuick", "Quick"),
                      tisch: t("v2.ratingTisch", "Tisch"),
                      setDefault: t("v2.ratingModeSetDefault", "Als Standard merken"),
                      cancel: t("v2.back", "Zurueck"),
                    }}
                    allowSetDefault
                    onSwitch={(next, makeDefault) => {
                      setSessionRatingMode(next);
                      if (makeDefault) handleSetPreferredMode(next);
                    }}
                  />
                </div>
              </div>
<<< ENDE NEU <<<

══════════════════════════════════════════════════════════
VERIFIKATION (lesend) — danach STOP
══════════════════════════════════════════════════════════
- `npx tsc --noEmit -p tsconfig.json` — keine NEUEN Fehler in LabsLive.tsx.
  (Bekannte node/vite-client/baseUrl-Meldungen ignorieren.) Insbesondere darf
  `freeRatingMode`/`setFreeRatingMode` NICHT mehr referenziert werden (sonst Fehler →
  Stelle melden, nicht raten).
- grep "effectiveFreeMode" → genau an: Definition, preferredMode, Chip mode. 
- grep "RatingModeChip" in LabsLive.tsx → Import + im Kopf verwendet.
- grep "freeRatingMode" → 0 Treffer.

STRIKT VERBOTEN:
- KEIN git add / commit / push. NICHT committen — nur Workspace-Dateien ändern.
- KEIN drizzle-kit, keine Schema-Änderung, keine Migration.
- Keine weiteren Dateien anfassen (RatingFlowV2.tsx, LabsTasteDrams.tsx bleiben
  unberührt — der Glas-Chip kommt erst in 2b weg).
Wenn etwas vom Step-0-Befund abweicht: NICHT raten — stoppen und mich fragen.
