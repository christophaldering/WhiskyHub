SAFETY TAG: git tag pre-profilmenu-info-back-2026-07-01

ZIEL:
Zwei kleine Verbesserungen:
FIX 1 (Info-Sektion): Im Profil-Menü (client/src/components/ProfileMenu.tsx)
sitzen "Über CaskSense" (about) und "Projekt unterstützen" (support)
aktuell unter der Sektion "KONTO & DATEN" (secAccount). Sie gehören
thematisch nicht dorthin. Eigene Sektion "INFO" (DE) / "INFO" (EN)
darunter anlegen und die beiden Einträge dorthin verschieben.
secAccount behält: E-Mail bestätigen, Daten & Export.

FIX 2 (Zurück-Konsistenz): LabsAbout nutzt beim Zurück den Fallback
"/labs/explore" (Discover) — obwohl About jetzt auch aus dem
Profil-Menü erreichbar ist. Wer aus dem Profil kommt, landet beim
Zurück falsch. Die Geschwister-Seiten (LabsTasteCooper, Settings,
Downloads) nutzen alle Fallback "/labs/taste/profile". useBackNavigation
nutzt ohnehin primär den navStack (letzte echte Route) — der Fallback
greift nur, wenn der Stack leer ist. Ziel: About soll sich konsistent
verhalten.

SCHRITT 0 — DIAGNOSE (nur lesen):
1. Zeige ProfileMenu.tsx die Label-Objekte (L, isDe/en, ~Z.246-260)
   und die main-View-Sektionen (~Z.810-915) — exakte Struktur der
   Sektions-Header + MenuButton-Reihenfolge.
2. Zeige LabsAbout.tsx Z.40-95 — den useBackNavigation-Aufruf
   (goBackToDiscover, Fallback "/labs/explore") und wo/wie der
   Button gerendert wird. Gibt es einen Grund für den
   Discover-Fallback (z.B. About wird primär aus Discover verlinkt)?
3. Bestätige: useBackNavigation(fallback) nutzt popRoute() (letzte
   Route aus navStack) und fällt nur bei leerem Stack auf fallback
   zurück — d.h. der navStack sorgt bereits dafür, dass man
   normalerweise dahin zurückkommt, wo man herkam. Der Fallback ist
   nur das Netz.

SCHRITT 1 — PLAN (bestätigen):
A) FIX 1 — Info-Sektion:
   - Neue Label-Keys secInfo: "Info" (DE) / "Info" (EN) in beiden
     Label-Objekten.
   - Die MenuButtons für about + support aus dem secAccount-Block
     herausnehmen und in einen neuen Sektions-Block mit Header
     L.secInfo verschieben, platziert NACH secAccount, VOR secAdmin.
   - secAccount behält verifyEmail + dataExport.
   - Keine Logik-Änderung an den Buttons selbst (navigate-Ziele
     bleiben).
B) FIX 2 — About Zurück konsistent:
   - Prüfen (aus Diagnose): Ist der Discover-Fallback gewollt, weil
     About historisch aus Discover kam? Wenn About jetzt primär
     Menü-Ziel ist, Fallback auf "/labs/taste/profile" ändern
     (wie die Geschwister-Seiten), damit bei leerem navStack das
     Verhalten konsistent ist.
   - WICHTIG: Da useBackNavigation ohnehin den navStack nutzt,
     ändert das nur das Verhalten im Randfall (leerer Stack /
     Direktaufruf). Der Normalfall (zurück wo man herkam) läuft
     bereits über popRoute.
   - Falls die Diagnose zeigt, dass About aus mehreren Kontexten
     kommt (Discover UND Profil), ist der navStack die richtige
     Lösung und nur der Fallback wird auf das häufigere/neutralere
     Ziel gesetzt. Plan entsprechend anpassen und benennen.

SCHRITT 2 — AUSFÜHRUNG: nach "OK, umsetzen".
Nur ProfileMenu.tsx + LabsAbout.tsx. Kein Schema, kein Server.

VERIFIKATION:
1. Profil-Menü zeigt neue Sektion "INFO" mit about + support;
   secAccount enthält nur noch verifyEmail + dataExport.
2. Reihenfolge: Cooper → Einstellungen → Darstellung → Konto&Daten
   → Info → (Admin). DE + EN Labels korrekt.
3. LabsAbout Zurück: aus Profil-Menü geöffnet → Zurück führt zum
   Profil (via navStack); Randfall leerer Stack → sinnvoller
   Fallback.
4. Andere Menü-Zielseiten (Cooper/Settings/Downloads) unverändert.
5. npx tsc --noEmit — keine neuen Fehler über Baseline (509).

KEIN COMMIT/PUSH. Christoph committet selbst.