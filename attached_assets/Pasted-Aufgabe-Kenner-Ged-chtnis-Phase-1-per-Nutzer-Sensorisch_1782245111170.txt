Aufgabe: Kenner-Gedächtnis Phase 1 — per-Nutzer „Sensorisches Gedächtnis" erzeugen, ansehen, löschen (Admin-Inspektion). KEINE Einspeisung in Cooper (das ist Phase 2).

SICHERHEITSREGELN: KEINE Commits, KEIN Push durch dich, KEIN drizzle-kit ausführen (Christoph macht den push manuell). Additive Schema-Spalten sind erlaubt (nur in schema.ts deklarieren, NICHT pushen). Sonst nichts an Migrationen/Schema.

Schritt 0 — Diagnose (NUR LESEN, dann auf mein OK warten):
1. git tag pre-kenner-memory-2026-06-23 setzen.
2. shared/schema.ts: die participants-Tabelle zeigen (Spalten cooper_entry_mode/cooper_level/cooper_depth als Muster) — wo additive Spalten eingefügt werden.
3. server/storage.ts: bestätige Signaturen von getJournalEntries, getParticipantStats, getVocabularyAdoption und der Methode zum Aktualisieren eines Participants (z. B. updateParticipant) — zeig deren Zeilen.
4. server/routes.ts: zeig das LLM-Client-Muster aus dem converse-Endpoint (OpenAI mit AI_INTEGRATIONS_OPENAI_API_KEY/BASE_URL, model "gpt-5-mini", reasoning_effort "minimal", max_completion_tokens) und bestätige, dass checkConverseRateLimit existiert.
5. client/src/labs/pages/LabsVoiceProbe.tsx: Admin-Gate (session.role !== "admin") bestätigen.

Plan (erst nach OK):

A) shared/schema.ts — zwei additive Spalten in participants (beim Muster der cooper_*-Spalten):
   cooperMemory: text("cooper_memory"),
   cooperMemoryUpdatedAt: timestamp("cooper_memory_updated_at"),
(Nur deklarieren. NICHT pushen, NICHT generate laufen lassen.)

B) server/routes.ts — drei Endpoints (in der Nähe der anderen Cooper-Routen):
GET /api/cooper/memory
- requireAuth; sonst Status.
- return res.json({ memory: (auth.participant as any).cooperMemory ?? null, updatedAt: (auth.participant as any).cooperMemoryUpdatedAt ?? null }).

POST /api/cooper/memory/generate
- requireAuth.
- Rate-Limit: checkConverseRateLimit("coopermem:" + auth.participant.id); bei !allowed → 429.
- Daten sammeln (nur eigene): entries = await storage.getJournalEntries(auth.participant.id); stats = await storage.getParticipantStats(auth.participant.id); vocab = await storage.getVocabularyAdoption(auth.participant.id).
- Aus entries die JÜNGSTEN max 60 nehmen; je Eintrag KOMPAKT serialisieren: Datum (YYYY-MM), Whisky-/Titel, Scores (nose/taste/finish/overall), tags, und tastingNarrative auf max 400 Zeichen gekürzt. vocab: die häufigsten ~40 Begriffe. stats: kompakt (Anzahl Drams, Schnitt, Spannbreite).
- LLM (gpt-5-mini, reasoning_effort "minimal", max_completion_tokens 900) mit diesem System-Prompt:
  "Du destillierst aus den Verkostungs-Daten EINER Person ein kompaktes 'Sensorisches Gedächtnis' — ein Porträt, WER sie als Taster ist. NUR aus den gelieferten Daten; erfinde nichts, ergänze keine Aromen. Schreibe in der zweiten Person ('Du neigst zu…'), Deutsch, 150–250 Wörter, zusammenhängender Text (KEINE Aufzählung). Erfasse: wiederkehrende Aromen/Noten, Gaumen-Tendenzen, wie sich dein Vokabular und deine Wahrnehmung über die Zeit verschieben, auffällige Vorlieben/Abneigungen, und Muster in deinen Bewertungen. Beschreibe Tendenzen, urteile nicht. Wenn die Datenbasis zu dünn ist, sag das knapp und ehrlich."
  User-Content: die kompakt serialisierten Daten.
- Ergebnis (getrimmt) in participant schreiben: storage.updateParticipant(auth.participant.id, { cooperMemory: text, cooperMemoryUpdatedAt: new Date() }) (exakte Methode/Feldnamen aus Schritt 0).
- return res.json({ memory: text, updatedAt: <jetzt> }).

DELETE /api/cooper/memory
- requireAuth.
- storage.updateParticipant(auth.participant.id, { cooperMemory: null, cooperMemoryUpdatedAt: null }).
- return res.json({ ok: true }).

C) client/src/labs/pages/LabsVoiceProbe.tsx — Sektion „Sensorisches Gedächtnis (Test)".
- Beim Laden GET /api/cooper/memory (pidHeaders) → memory + updatedAt in State.
- Anzeige: der Gedächtnis-Text (Cormorant/serif, ruhig) oder Hinweis „Noch nicht erzeugt."; darunter updatedAt klein.
- Knopf „Neu erzeugen" → POST /api/cooper/memory/generate (Ladezustand „Verdichte …"); Ergebnis in State.
- Knopf „Löschen" → DELETE /api/cooper/memory → State leeren.
- Kurzer Hinweis: „Phase 1: Nur zur Prüfung. Cooper nutzt dies noch nicht — später ausschließlich im Nachgang (Kenner), nie am Glas."
- Styling im Stil der Seite; keine neue Lib.

D) i18n: nicht nötig (Admin-Test-Screen, deutsche Inline-Strings genügen).

Verifikation (zeig grep-Ausgaben):
1. grep -n "cooper_memory\|cooperMemory" shared/schema.ts → zwei Spalten.
2. grep -n "/api/cooper/memory" server/routes.ts → drei Endpoints.
3. grep -n "Sensorisches Gedächtnis\|cooper/memory\|Neu erzeugen" client/src/labs/pages/LabsVoiceProbe.tsx.
4. Typecheck sauber.
5. WICHTIG: erinnere ausdrücklich daran, dass die neuen Spalten erst nach manuellem `npx drizzle-kit push --force` in der Replit-Shell existieren — und dass DU (Agent) das NICHT ausführst.
6. git log --oneline -3 + git status → NICHTS committet/gepusht; Tag pre-kenner-memory-2026-06-23 existiert. Bestätige: „Keine Commits, kein Push, kein drizzle-kit ausgeführt."

Rollback: git reset --hard pre-kenner-memory-2026-06-23.
