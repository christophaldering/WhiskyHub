HARD PROMPT — Cooper-Stimme auf Konten beschränken + Gast-Anmelde-Hinweis

SICHERHEITSREGELN (für den GESAMTEN Prompt):
1. Zuerst NUR Step 0 (Diagnose), keine Änderung, bis ich den Plan bestätige.
2. Plan zeigen, auf meine Bestätigung warten, DANN ausführen.
3. NICHT committen: kein git add, kein git commit, kein git push.
4. KEINE Schema-/Migrations-Änderung, kein drizzle-kit. (Reine Logik-/UI-Edits.)
5. Nur diese 3 Dateien anfassen: server/routes.ts, client/src/labs/components/rating/ImpressionCapture.tsx, client/src/lib/i18n.ts
Safety-Tag vorab:  git tag pre-cooper-voice-gate-2026-06-23
Rollback:          git reset --hard pre-cooper-voice-gate-2026-06-23

ENTSCHEIDUNG (so gewollt): Echtzeit-STIMME nur für registrierte Konten (E-Mail) + Admin.
Tipp-Cooper (/api/impression/converse) bleibt OFFEN (billig, auch für Gruppen-Gäste).
Gäste sehen in Solo einen Anmelde-Hinweis statt des Sprich-Knopfs; "Lieber tippen" + "Direkt bewerten" bleiben.

STEP 0 — DIAGNOSE, bestätige durch Lesen (keine Änderung):
1. server/routes.ts ~Z.5598: in POST /api/voice-probe/token steht
   if (auth.participant.role !== "admin") return res.status(403).json({ message: "Admin access required" });
2. participants haben ein Feld email (Gäste = ohne E-Mail; registriert = mit E-Mail).
3. ImpressionCapture.tsx: nutzt useCooperVoice(); importiert FONT, SP, RADIUS, TOUCH_MIN, LABS_THEME aus "./theme";
   Voice-Phase (nicht verbunden) rendert den Connect-Button (voice.connect(), Label v2.voiceStart),
   darunter die KI-Notiz (v2.voiceAiNote), dann "Lieber tippen" (v2.voicePreferType) und "Direkt bewerten" (v2.impressionSkip).
4. STOPP, zeig mir den Plan.

PLAN (erst nach Bestätigung):

--- EDIT 1: server/routes.ts (voice-probe/token) ---
Ersetze in POST /api/voice-probe/token die Admin-Zeile
  if (auth.participant.role !== "admin") return res.status(403).json({ message: "Admin access required" });
durch:
  const voiceAllowed = auth.participant.role === "admin" || !!(auth.participant.email && auth.participant.email.trim());
  if (!voiceAllowed) return res.status(403).json({ code: "registration_required", message: "Mit Cooper sprechen gibt es mit einem kostenlosen Konto. Melde dich an, um Coopers Stimme zu nutzen." });
(converse-Endpoint NICHT anfassen.)

--- EDIT 2: ImpressionCapture.tsx ---
2a. Imports oben ergänzen:
    import { useQuery } from "@tanstack/react-query";
    import { participantApi, getParticipantId } from "@/lib/api";
    import { useAppStore } from "@/lib/store";

2b. In der Komponente (oben bei den Hooks) ergänzen:
    const _pid = participantId || getParticipantId() || "";
    const { data: _me } = useQuery<{ id: string; email?: string | null }>({
      queryKey: ["impression-voice-me", _pid],
      queryFn: () => participantApi.get(_pid),
      enabled: !!_pid,
      staleTime: 5 * 60 * 1000,
    });
    const openAuthDialog = useAppStore((s) => s.openAuthDialog);
    const voiceLocked = !!_me && !_me.email;   // registriert = hat E-Mail

2c. In der Voice-Phase den Connect-Button + die KI-Notiz (v2.voiceAiNote) in eine Bedingung wickeln:
    {voiceLocked ? (
      <div style={{ marginBottom: SP.md, padding: SP.md, borderRadius: RADIUS.md, border: `1px solid ${LABS_THEME.border}`, background: LABS_THEME.bgCard, textAlign: "center" }}>
        <div style={{ fontFamily: FONT.body, fontSize: 15, color: LABS_THEME.text, lineHeight: 1.5, marginBottom: SP.sm }}>
          {t("v2.voiceLockedHint", "Cooper zum Sprechen gibt es mit einem kostenlosen Konto.")}
        </div>
        <button type="button" onClick={() => openAuthDialog("register")} data-testid="impression-voice-signup"
          style={{ background: "none", border: "none", color: LABS_THEME.gold, fontFamily: FONT.body, fontSize: 15, fontWeight: 600, cursor: "pointer", textDecoration: "underline", textUnderlineOffset: 3, minHeight: TOUCH_MIN }}>
          {t("v2.voiceLockedCta", "Konto erstellen")}
        </button>
      </div>
    ) : (
      <>
        {/* HIER unverändert: bestehender Connect-Button (voice.connect(), v2.voiceStart) + KI-Notiz (v2.voiceAiNote) */}
      </>
    )}
    WICHTIG: "Lieber tippen" (v2.voicePreferType) und "Direkt bewerten" (v2.impressionSkip) bleiben AUSSERHALB
    der Bedingung — also für Gäste weiterhin sichtbar. Nur Connect-Button + KI-Notiz wandern in den :-Zweig.

--- EDIT 3: i18n.ts (EN-Keys ergänzen, im englischen v2-Block ~Z.781) ---
    voiceLockedHint: "Talking to Cooper comes with a free account.",
    voiceLockedCta: "Create an account",
(Deutsch kommt aus den Inline-Fallbacks in Edit 2c.)

VERIFIKATION:
- Server: voice-probe/token erlaubt jetzt Admin ODER Teilnehmer mit E-Mail; Gast → 403 code "registration_required".
- converse-Endpoint unverändert (Tippen bleibt für alle offen).
- ImpressionCapture: als Gast (Teilnehmer ohne E-Mail) erscheint in der Voice-Phase der Hinweis + "Konto erstellen" (öffnet Registrierung) statt "Mit Cooper sprechen"; "Lieber tippen" und "Direkt bewerten" sind weiter da. Als registrierter Nutzer/Admin unverändert "Mit Cooper sprechen".
- Nur 3 Dateien geändert. Typecheck/Build ohne neue Fehler.
- NICHT committen. Kein add/commit/push, kein drizzle-kit. Nur im Arbeitsbaum lassen.
