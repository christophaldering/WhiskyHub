# HARD PROMPT — CaskSense: Host-Cockpit Gruppen-Header Überlappung beheben

## Ziel
Im Host-Cockpit (Tab „Gruppe") überläuft bei langem Whisky-Namen der Name den Titel „GRUPPENBEWERTUNGEN". Fix: Titel nicht schrumpfen lassen, Whisky-Name mit Ellipsis abschneiden. **Eine Datei, nur CSS/Inline-Style.**

## WICHTIG
- **SCHRITT 0 zuerst (NUR LESEN):** Anker bestätigen, kurz melden, **auf mein OK warten.**
- **KEIN commit. KEIN git. KEINE Schema-/drizzle-Änderung.** Nur `client/src/labs/pages/LabsHostCockpit.tsx`.

## SCHRITT 0 — Diagnose
Bestätige in `client/src/labs/pages/LabsHostCockpit.tsx` den CSS-Block `.cockpit-card-title { … gap: 8px; }` und in `renderGroupStats` den `<span style={{ fontSize: 12, fontWeight: 600, color: "var(--labs-text-muted)" }}>` mit dem `isBlind ? … : (activeWhisky.name …)`-Inhalt. **Dann auf OK warten.**

## SCHRITT 1 — CSS: Titel nicht schrumpfen
>>> ALT >>>
```
        .cockpit-card-title {
          font-size: 12px;
          font-weight: 700;
          color: var(--labs-text-muted);
          letter-spacing: 0.04em;
          text-transform: uppercase;
          display: flex;
          align-items: center;
          gap: 8px;
        }
```
<<< ENDE ALT <<<
>>> NEU >>>
```
        .cockpit-card-title {
          font-size: 12px;
          font-weight: 700;
          color: var(--labs-text-muted);
          letter-spacing: 0.04em;
          text-transform: uppercase;
          display: flex;
          align-items: center;
          gap: 8px;
          flex-shrink: 0;
        }
```
<<< ENDE NEU <<<

## SCHRITT 2 — Whisky-Name truncaten
>>> ALT >>>
```
            <span style={{ fontSize: 12, fontWeight: 600, color: "var(--labs-text-muted)" }}>
              {isBlind ? `Dram ${blindLabel(effectiveDramIdx)}` : (activeWhisky.name || `Dram ${effectiveDramIdx + 1}`)}
            </span>
```
<<< ENDE ALT <<<
>>> NEU >>>
```
            <span style={{ fontSize: 12, fontWeight: 600, color: "var(--labs-text-muted)", minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", textAlign: "right", marginLeft: 8 }}>
              {isBlind ? `Dram ${blindLabel(effectiveDramIdx)}` : (activeWhisky.name || `Dram ${effectiveDramIdx + 1}`)}
            </span>
```
<<< ENDE NEU <<<

## VERIFIKATION
- `.cockpit-card-title` hat `flex-shrink: 0`.
- Der Whisky-Name-span hat die Truncate-Styles.
- TypeScript bleibt sauber.
- **KEIN commit, KEIN git, KEINE Schema-Änderung. Eine Datei.**
