HARD PROMPT — Hero griffiger: „für wen"-Kicker + neue Nutzen-Zeile (landing-x)

ZIEL: Zwei Hero-Eingriffe in client/src/pages/landing-x.tsx. Claim bleibt unverändert.
KEIN Schema, KEIN drizzle, KEIN Commit/Push.

ROLLBACK-TAG ZUERST:
  git tag pre-hero-griffig-2026-06-26

STEP 0 — DIAGNOSE: Bestätige in client/src/pages/landing-x.tsx das Vorhandensein von
`data-testid="hero-lead"`, der Zeile `Whisky bewusst verkosten — und in Worte fassen, was du schmeckst. Allein oder in der Runde.`
und direkt vor dem Wordmark `<FadeUp delay={0.15}>` gefolgt von `<h1`. Dann STOPP + warte auf "OK".

NACH OK — Edits NUR in client/src/pages/landing-x.tsx:

EDIT 1 — „Für wen"-Kicker über dem Schriftzug einfügen.
SUCHE:
      <FadeUp delay={0.15}>
        <h1
ERSETZE:
      <FadeUp delay={0.1}>
        <p
          style={{
            fontFamily: font.body,
            fontSize: "clamp(10px, 1.3vw, 12px)",
            fontWeight: 600,
            letterSpacing: "0.18em",
            textTransform: "uppercase",
            color: ACCENT_DIM,
            marginBottom: 18,
            position: "relative",
            zIndex: 2,
          }}
          data-testid="hero-kicker"
        >
          Für alle, die Whisky mehr als nur trinken
        </p>
      </FadeUp>

      <FadeUp delay={0.15}>
        <h1

EDIT 2 — Nutzen-Zeile griffiger + präsenter.
SUCHE:
            fontSize: "clamp(15px, 1.7vw, 18px)",
            fontWeight: 300,
            lineHeight: 1.55,
            color: v.mutedLight,
            maxWidth: 460,
            margin: "0 auto 40px",
            position: "relative",
            zIndex: 2,
          }}
          data-testid="hero-lead"
        >
          Whisky bewusst verkosten — und in Worte fassen, was du schmeckst. Allein oder in der Runde.
        </p>
ERSETZE:
            fontSize: "clamp(16px, 2vw, 21px)",
            fontWeight: 400,
            lineHeight: 1.5,
            color: v.muted,
            maxWidth: 500,
            margin: "0 auto 40px",
            position: "relative",
            zIndex: 2,
          }}
          data-testid="hero-lead"
        >
          Verkosten, in Worte fassen, vergleichen — und mit der Zeit deinen Geschmack verstehen.
        </p>

VERIFIKATION (nur Ausgabe, NICHT committen/pushen):
  grep -c "hero-kicker" client/src/pages/landing-x.tsx                          # 1
  grep -c "Für alle, die Whisky mehr als nur trinken" client/src/pages/landing-x.tsx  # 1
  grep -c "und mit der Zeit deinen Geschmack verstehen" client/src/pages/landing-x.tsx # 1
  Danach Build-/tsc-Check, dass landing-x.tsx fehlerfrei kompiliert.
COMMIT-VERBOT: NICHT add/commit/push, NICHT drizzle, KEINE Schema-Änderung. Push mache ich selbst.