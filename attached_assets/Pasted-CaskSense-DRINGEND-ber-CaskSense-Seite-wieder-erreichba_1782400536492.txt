CaskSense — DRINGEND: Über-CaskSense-Seite wieder erreichbar machen (Routing war verwaist nach Sektion-Entfernung).
Datei: client/src/App.tsx  (NUR diese Datei)

═══ SICHERHEIT ZUERST ═══
Rollback-Tag:  git tag pre-about-rehome-2026-06-25
VERBOTEN: kein git commit, kein git push, kein drizzle-kit, keine Schema-/Migrations-Änderung. Nur Datei-Edits.

═══ SCHRITT 0 — DIAGNOSE (nur lesen) ═══
Bestätige in client/src/App.tsx:
- Es gibt `const LabsAbout = lazy(() => import("@/labs/pages/LabsAbout"));` (~Z.125).
- `Redirect` wird bereits verwendet (z.B. ~Z.631 `<Redirect to="/labs/about" />`).
- Die Routen ~Z.791/792 für `/labs/about` und `/labs/donate` leiten per RedirectWithQuery auf `tab=bibliothek&section=ueber-casksense&sub=about` um.
- Der String `"/labs/explore?tab=bibliothek&section=ueber-casksense&sub=about"` kommt in der Redirect-Map genau 7× vor.
Gib mir Ist-Stand + Plan. Warte auf mein "go".

═══ SCHRITT 1 — ÄNDERN (erst nach "go") ═══

1a) Ersetze die Route-Zeile für /labs/about:

              <Route path="/labs/about">{() => <RedirectWithQuery to="/labs/explore" query="tab=bibliothek&section=ueber-casksense&sub=about" />}</Route>

   DURCH:

              <Route path="/labs/about" component={LabsAbout} />

1b) Ersetze die Route-Zeile für /labs/donate:

              <Route path="/labs/donate">{() => <RedirectWithQuery to="/labs/explore" query="tab=bibliothek&section=ueber-casksense&sub=about" />}</Route>

   DURCH:

              <Route path="/labs/donate">{() => <Redirect to="/labs/about" />}</Route>

1c) In der Redirect-Map: ersetze ALLE Vorkommen des Wert-Strings

   "/labs/explore?tab=bibliothek&section=ueber-casksense&sub=about"

   DURCH:

   "/labs/about"

   (Das betrifft genau 7 Einträge: /discover/about, /discover/donate, /profile/help, /help, /about, /features, /donate. Die Schlüssel links NICHT ändern, nur den Wert rechts.)

═══ SCHRITT 2 — VERIFIKATION (zeig mir die Ausgabe) ═══
- grep -n "ueber-casksense" client/src/App.tsx                 → MUSS 0 Treffer sein
- grep -c "\"/labs/about\"" client/src/App.tsx                 → erwartet ≥ 8 (Route + 7 Map-Einträge; m2-Redirects zählen extra)
- grep -n "path=\"/labs/about\" component={LabsAbout}" client/src/App.tsx → 1 Treffer
- Projekt-Typecheck/Build (npm run check bzw. tsc --noEmit)    → KEINE Fehler
- Manuell im Browser: /labs/about zeigt die Über-CaskSense-Seite (Text, Foto, Spende). /donate landet ebenfalls dort.
ERINNERUNG: kein commit, kein push, kein drizzle, keine Schema-Änderung.
