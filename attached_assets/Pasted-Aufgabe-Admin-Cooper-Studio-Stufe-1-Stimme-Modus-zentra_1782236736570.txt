Aufgabe: Admin-Cooper-Studio Stufe 1 — Stimme + Modus zentral als globalen Default setzen, ohne Hard Prompt.

Ziel: Im Admin-Screen (LabsVoiceProbe) Cooper-Stimme + Modus wählen, live testen, und als globalen Default speichern (app_settings, KEINE Schema-Änderung). Endnutzer bekommen den globalen Default; Admin kann im Studio per Flag probe:true zum Testen überschreiben. Ohne gesetzten Default bleibt cedar/tiefsinnig (heutiges Verhalten).

SICHERHEITSREGELN: KEINE Commits, KEIN Push, KEIN drizzle-kit, KEINE Schema-/Migrationsänderung (app_settings existiert bereits). Reiner Code-Change in routes.ts + useCooperVoice.ts + LabsVoiceProbe.tsx.

Schritt 0 — Diagnose (NUR LESEN, dann auf mein OK warten):
1. git tag pre-cooper-studio-2026-06-23 setzen.
2. Zeige server/routes.ts Z. 5594–5612 (Auth-Teil von /api/voice-probe/token) und Z. 5633–5641 (VOICES/voice/mode/candidates).
3. Zeige client/src/labs/components/rating/useCooperVoice.ts Z. 14–22 (Signatur/Defaults) und die Zeile mit fetch("/api/voice-probe/token" ...).
4. Zeige client/src/labs/pages/LabsVoiceProbe.tsx Z. 1–80.
5. Bestätige per grep, dass storage.getAppSetting / storage.setAppSetting existieren.

Plan (erst nach OK ausführen):

A) server/routes.ts — globalen Default im Token-Endpoint nutzen.
Ersetze in /api/voice-probe/token die beiden Zeilen:
ALT:
      const voice = VOICES.includes(req.body?.voice) ? req.body.voice : "cedar";
      const mode = ["fluessig", "tiefsinnig"].includes(req.body?.mode) ? req.body.mode : "tiefsinnig";
NEU:
      const MODES = ["fluessig", "tiefsinnig"];
      const isAdminCaller = auth.participant.role === "admin";
      const isProbe = req.body?.probe === true;
      const gVoice = await storage.getAppSetting("cooper_default_voice");
      const gMode = await storage.getAppSetting("cooper_default_mode");
      const defVoice = (gVoice && VOICES.includes(gVoice)) ? gVoice : "cedar";
      const defMode = (gMode && MODES.includes(gMode)) ? gMode : "tiefsinnig";
      const voice = (isProbe && isAdminCaller && VOICES.includes(req.body?.voice)) ? req.body.voice : defVoice;
      const mode = (isProbe && isAdminCaller && MODES.includes(req.body?.mode)) ? req.body.mode : defMode;
(VOICES bleibt unverändert direkt darüber. Die langen Cooper-Instructions NICHT anfassen.)

B) server/routes.ts — zwei neue Admin-Endpoints (in der Nähe anderer /api/admin-Routen einfügen).
GET /api/admin/cooper-defaults:
- requireAuth; wenn nicht authentifiziert → entsprechender Status.
- wenn auth.participant.role !== "admin" → 403 { message: "Admin access required" }.
- gV = await storage.getAppSetting("cooper_default_voice"); gM = await storage.getAppSetting("cooper_default_mode");
- res.json({ voice: gV ?? "cedar", mode: gM ?? "tiefsinnig" }).
POST /api/admin/cooper-defaults:
- gleiche Admin-Prüfung wie oben.
- VOICES = ["alloy","ash","ballad","coral","echo","sage","shimmer","verse","marin","cedar"]; MODES = ["fluessig","tiefsinnig"].
- wenn req.body.voice nicht in VOICES ODER req.body.mode nicht in MODES → 400 { message: "Invalid voice or mode." }.
- await storage.setAppSetting("cooper_default_voice", req.body.voice); await storage.setAppSetting("cooper_default_mode", req.body.mode).
- res.json({ voice: req.body.voice, mode: req.body.mode }).

C) client/src/labs/components/rating/useCooperVoice.ts — Probe-Flag mitsenden.
- Signatur erweitern: opts?: { initialVoice?: string; initialMode?: "fluessig" | "tiefsinnig"; probe?: boolean }
- In der fetch("/api/voice-probe/token", ...)-Zeile den Body ändern:
  ALT: body: JSON.stringify({ voice, mode })
  NEU: body: JSON.stringify({ voice, mode, probe: opts?.probe === true })
(Sonst nichts am Hook ändern. Der echte Flow ruft useCooperVoice() ohne probe → probe:false → Server nimmt globalen Default.)

D) client/src/labs/pages/LabsVoiceProbe.tsx — zum Studio erweitern.
- useCooperVoice() → useCooperVoice({ probe: true }).
- Beim Laden (useEffect, einmalig): GET /api/admin/cooper-defaults (mit pidHeaders), und mit setVoice(data.voice) + setMode(data.mode) die aktuell gespeicherte Stimme/Modus vorbelegen.
- Einen Knopf "Als globalen Default speichern" ergänzen: POST /api/admin/cooper-defaults mit { voice, mode } (aktueller Hook-Zustand) + pidHeaders; bei Erfolg ein kurzer Hinweis "Gespeichert" (vorhandenes Toast-Muster, sonst einfacher Text/State).
- Eine Zeile anzeigen: "Aktueller globaler Default: {gespeicherteStimme} · {gespeicherterModus}" (aus dem GET), die sich nach dem Speichern aktualisiert.
- Styling im Stil der bestehenden Chips/Seite; keine neue Lib.

Verifikation (zeig grep-Ausgaben):
1. grep -n "cooper_default_voice\|cooper_default_mode\|isProbe\|defVoice" server/routes.ts → Token-Logik + beide Endpoints vorhanden.
2. grep -n "probe" client/src/labs/components/rating/useCooperVoice.ts → Signatur + Body.
3. grep -n "cooper-defaults\|probe: true\|Als globalen Default" client/src/labs/pages/LabsVoiceProbe.tsx.
4. Typecheck sauber.
5. Funktionsprobe-Beschreibung (kein Bau): erklären, wie ein Endnutzer ohne probe den Default bekommt und der Admin mit probe:true testet.
6. git log --oneline -3 + git status → NICHTS committet/gepusht; Tag pre-cooper-studio-2026-06-23 existiert. Bestätige: "Keine Commits, kein Push, keine Schema-Änderung."

Rollback: git reset --hard pre-cooper-studio-2026-06-23.
