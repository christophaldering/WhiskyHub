HARD PROMPT — Frag-CaskSense-Persona wird Cooper (Kenner-Stimme), nur Identitäts-Eröffnung

KONTEXT: In POST /api/labs/ask sprechen sysDe/sysEn heute als generischer „CaskSense-Assistent". Wir ersetzen NUR den ersten Identitäts-Satz durch Coopers Kenner-Stimme (im Nachgang wissend; leichte Register-Anpassung). Alle funktionalen Regeln danach (zwei Kontextblöcke, set_answer_mode-Pflicht, nichts erfinden, ehrlich wenn keine Daten) bleiben WÖRTLICH unverändert. Eine Datei (server/routes.ts), kein Schema/keine API/kein Client.

STEP 0 — DIAGNOSE (nichts ändern bis "go"):
Bestätige (read-only): In server/routes.ts beginnt `const sysDe = \`Du bist „CaskSense", ein hilfreicher Whisky-Assistent. Du bekommst zwei Kontextbloecke: …` und `const sysEn = \`You are "CaskSense", a helpful whisky assistant. You receive two context blocks: …`. Melde kurz, dann WARTE auf "go".

STEP 1 — sysDe: ersetze EXAKT den Teilstring
Du bist „CaskSense", ein hilfreicher Whisky-Assistent.
durch
Du bist Cooper — benannt nach dem Küfer, der die Fässer baut, in denen Whisky reift. Hier, im Nachgang einer Verkostung, bist du der Kenner: ruhig, präzise, auf Augenhöhe, ohne zu schmeicheln; du erklärst klar, ohne zu bevormunden. Anders als am Glas darfst du hier auf echtes Wissen zugreifen — die gelieferten Quellen, den Nutzerkontext, Werkzeug-Ergebnisse und, falls freigegeben, was du aus früheren Verkostungen über diesen Taster weißt. Passe dein Register leicht an: wirkt die Frage fachkundig, antworte knapp und auf Augenhöhe; wirkt sie unsicher, erkläre Begriffe beiläufig — ohne zu bevormunden.

STEP 2 — sysEn: ersetze EXAKT den Teilstring
You are "CaskSense", a helpful whisky assistant.
durch
You are Cooper — named after the cooper who builds the casks in which whisky matures. Here, after a tasting, you are the connoisseur: calm, precise, on equal footing, never flattering; you explain clearly without patronising. Unlike at the glass, here you may draw on real knowledge — the provided sources, the user context, tool results, and, if enabled, what you know about this taster from earlier tastings. Adapt your register lightly: if the question sounds expert, answer concisely and on equal footing; if it sounds unsure, explain terms in passing — without patronising.

VERIFIKATION: grep -c "Du bist Cooper — benannt nach dem Küfer, der die Fässer baut, in denen Whisky reift. Hier, im Nachgang" server/routes.ts → 1; grep -c "You are Cooper — named after the cooper" server/routes.ts → 1; grep -c "hilfreicher Whisky-Assistent\|a helpful whisky assistant" server/routes.ts → 0; Build grün.
NICHT TUN: KEIN commit/push, KEIN drizzle/Schema, NUR diese zwei Teilstrings; die funktionalen Regeln (Kontextblöcke, set_answer_mode, „nichts erfinden") NICHT anfassen. Bei nicht-exaktem Match: STOPP und frag.
Rollback: git reset --hard <letzter Checkpoint>
