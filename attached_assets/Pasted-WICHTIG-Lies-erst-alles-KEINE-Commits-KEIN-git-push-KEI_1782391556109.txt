WICHTIG: Lies erst alles. KEINE Commits, KEIN git push, KEIN drizzle-kit, KEINE Schema-/Migrationsänderung. Zwei Dateien: client/src/labs/labs-theme.css und client/src/labs/pages/LabsCircle.tsx.

SICHERHEITS-TAG ZUERST (Shell):
  git tag pre-circle-2x3-2026-06-25

SCHRITT 0 — DIAGNOSE (nur lesen, zeigen):
  1. LabsCircle.tsx: bestätige `const tilesRow1 = tiles.slice(0, 3);` / `const tilesRow2 = tiles.slice(3, 6);` und den Render-Block mit ZWEI <div className="labs-action-bar"> (tilesRow1.map / tilesRow2.map).
  2. labs-theme.css: bestätige die Regel `.labs-action-bar-item + .labs-action-bar-item { border-left: 1px solid var(--labs-border-subtle); }`.
  Zeig kurz das Ergebnis.

EDIT 1 — client/src/labs/labs-theme.css: Modifier für ein 2×3-Feld in EINEM Rahmen, ohne innere Trennlinien.
FINDE:
.labs-action-bar-item + .labs-action-bar-item {
  border-left: 1px solid var(--labs-border-subtle);
}
ERSETZE DURCH:
.labs-action-bar-item + .labs-action-bar-item {
  border-left: 1px solid var(--labs-border-subtle);
}

/* Circle: sechs gleichrangige Erst-Ebenen-Punkte als ein 2×3-Feld, ohne Zwischenfächer */
.labs-action-bar--grid-2x3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
}
.labs-action-bar--grid-2x3 .labs-action-bar-item + .labs-action-bar-item {
  border-left: none;
}

EDIT 2 — client/src/labs/pages/LabsCircle.tsx: die zwei Reihen-Container durch EINEN 2×3-Grid-Container ersetzen.
FINDE:
        <div className="labs-action-bar">
          {tilesRow1.map(renderTile)}
        </div>
        <div className="labs-action-bar">
          {tilesRow2.map(renderTile)}
        </div>
ERSETZE DURCH:
        <div className="labs-action-bar labs-action-bar--grid-2x3">
          {tiles.map(renderTile)}
        </div>

EDIT 3 — selbe Datei: die nun ungenutzten Slice-Definitionen entfernen.
FINDE:
  const tilesRow1 = tiles.slice(0, 3);
  const tilesRow2 = tiles.slice(3, 6);
ERSETZE DURCH:
(nichts — beide Zeilen ersatzlos löschen)

VERIFIKATION (nur lesen, zeigen):
  1. grep -c "labs-action-bar--grid-2x3" client/src/labs/labs-theme.css → 2
  2. grep -c "labs-action-bar--grid-2x3" client/src/labs/pages/LabsCircle.tsx → 1
  3. grep -c "tilesRow1\|tilesRow2" client/src/labs/pages/LabsCircle.tsx → 0
  4. grep -c "{tiles.map(renderTile)}" client/src/labs/pages/LabsCircle.tsx → 1
  5. npx tsc --noEmit → keine NEUEN Fehler in den zwei Dateien

ERINNERUNG ZUM SCHLUSS: KEIN commit, KEIN push, KEIN drizzle-kit, KEINE Schema-/Migrationsänderung. Nur die genannten Stellen. Zeig mir die Verifikations-Ergebnisse.
