PHASE 1 ONLY — NO IMPLEMENTATION YET.

TASK:
Analyze the current CaskSense codebase architecture relevant to:
- Tasting Room (/tasting/:id)
- Naked View (/naked/:code if present)
- Session entry flows (/join/:code, sessions list, host dashboard links)
- Layout components that render Sidebar, Mobile Hamburger Sheet, and Bottom Navigation
- Any existing “blind reveal” logic and permission gating (host/admin/analyst levels)

GOAL:
Produce a clear technical summary so we can implement “Naked-first Progressive Reveal” safely in Phase 2.

STRICT RULE:
Do NOT change any code. Do NOT refactor. Do NOT create PRs. Do NOT add files. Do NOT run migrations.
This is analysis + plan only.

DELIVERABLES (MANDATORY):
1) ROUTE MAP
   - List all relevant routes and where they are defined (file paths).
   - Describe how a user arrives at /tasting/:id (all entry points).
   - Confirm whether /naked/:code exists, and how it is used.

2) LAYOUT / NAV ARCHITECTURE
   - Identify the root layout / app shell component(s).
   - Explain exactly where Sidebar, Mobile Sheet menu, and Bottom Nav are rendered.
   - Explain how layout suppression could be achieved for Naked Mode (e.g., conditional layout wrapper, route-based layout, state flag).

3) TASTING ROOM STRUCTURE
   - Identify main components for tasting room UI.
   - Describe current state management (React state, context, Redux/Zustand, URL params, server state, etc.).
   - Identify how “current whisky/expression”, rating form, notes, and flight board are handled.

4) BLIND / REVEAL LOGIC
   - Where is blind mode enforced?
   - How does reveal phase progression work (if present)?
   - Where are whisky details blocked/unblocked?

5) PERMISSIONS / EXPERIENCE LEVEL GATING
   - Where are roles/levels stored and checked?
   - Which parts are currently gated (analytics, database, exports, admin)?
   - Identify risks of permission leaks in a new overlay/drawer.

6) RISK ASSESSMENT + RECOMMENDED IMPLEMENTATION PLAN
   - Top 5 risks (e.g., routing loops, nav duplication, state resets, mobile modal issues, reveal leakage).
   - A minimal-change implementation plan for Phase 2:
     a) where to hook “Naked-first”
     b) how to suppress global navigation
     c) how to implement the Progressive Reveal sheet (Context/Comparison/Pro Tools) without touching core data models
   - A QA checklist tailored to the current codebase.

OUTPUT FORMAT:
Provide the result as a structured markdown document with headings matching items 1–6 above.
Include file paths, component names, and brief code references (no huge code dumps).