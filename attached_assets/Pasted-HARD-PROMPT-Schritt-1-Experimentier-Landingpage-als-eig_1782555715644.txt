HARD PROMPT — Schritt 1: Experimentier-Landingpage als eigener Strang (/landing-x)

ZIEL: Eine separate, identische Kopie der aktuellen Landingpage unter neuer Route /landing-x,
damit wir dort gefahrlos die neue Konzeption entwickeln können. Die Live-Seite (/) und
landing-new.tsx bleiben UNVERÄNDERT. KEIN Schema, KEIN drizzle.

ROLLBACK-TAG ZUERST:
  git tag pre-landing-x-2026-06-26

STEP 0 — DIAGNOSE (nur lesen). Bestätige per Ausgabe:
  - client/src/pages/landing-new.tsx existiert.
  - client/src/App.tsx hat die Zeile `const LandingNew = lazy(() => import("@/pages/landing-new"));`
    und die Route `<Route path="/landing-static" component={LandingNew} />`.
  Zeig mir das + PLAN, dann STOPP und warte auf mein "OK".

NACH MEINEM OK — genau diese 3 Schritte:

EDIT 1 — Datei duplizieren:
  Erstelle client/src/pages/landing-x.tsx als EXAKTE, inhaltsgleiche Kopie von
  client/src/pages/landing-new.tsx (gleicher Inhalt, Zeichen für Zeichen). Ändere nichts am Inhalt.
  (Shell-Variante erlaubt: `cp client/src/pages/landing-new.tsx client/src/pages/landing-x.tsx`.)

EDIT 2 — Import in client/src/App.tsx:
  SUCHE (exakt):
const LandingNew = lazy(() => import("@/pages/landing-new"));
  ERSETZE:
const LandingNew = lazy(() => import("@/pages/landing-new"));
const LandingX = lazy(() => import("@/pages/landing-x"));

EDIT 3 — Route in client/src/App.tsx:
  SUCHE (exakt):
        <Route path="/landing-static" component={LandingNew} />
  ERSETZE:
        <Route path="/landing-static" component={LandingNew} />
        <Route path="/landing-x" component={LandingX} />

VERIFIKATION (nur Ausgabe, NICHT committen/pushen):
  test -f client/src/pages/landing-x.tsx && echo "landing-x da"
  diff -q client/src/pages/landing-new.tsx client/src/pages/landing-x.tsx && echo "identisch"
  grep -c "landing-x" client/src/App.tsx        # erwartet 2 (Import + Route)
  Danach: tsc/Build-Check, dass App.tsx und landing-x.tsx fehlerfrei kompilieren.

COMMIT-VERBOT (nochmal): NICHT git add, NICHT git commit, NICHT git push, NICHT drizzle,
KEINE Schema-Änderung. Nur Dateien anlegen/ändern + Verifikation zeigen. Push mache ich selbst.
