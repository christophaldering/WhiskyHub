ROLE: You are the strictest UI consistency auditor + minimal patcher. You must NOT refactor. You must NOT change routing, data fetching, business logic, or component architecture. Only apply SMALL, LOCAL, SAFE UI fixes (classes, spacing, wrapper components) to enforce visual consistency.

GOAL: Run a Micro-Consistency Audit across the app’s main user-facing pages and apply minimal patches to remove visual inconsistencies. The reference look & feel is /my/journal and /tasting/calendar (the “Journal-System” layout).

HARD RULES:
- Do NOT rename files, do NOT move folders, do NOT introduce new design systems.
- Do NOT rewrite components into new abstractions.
- Prefer changing Tailwind classes or swapping to existing shared components already in the repo (e.g., PageLayout) ONLY if it is already used elsewhere.
- Keep changes minimal and reversible.
- All pages must still compile and behave identically (only look/spacing improves).
- /profile is explicitly excluded from forced PageLayout/container changes (it is intentionally narrower).

PHASE 0 — INVENTORY (NO CHANGES YET)
1) Identify the “reference tokens” used by /my/journal and /tasting/calendar:
   - main container max width (e.g., max-w-5xl)
   - horizontal padding classes
   - header block structure (icon size, H1 classes, subline classes, spacing)
   - card classes (rounded, border/shadow, padding)
   - tabs/segmented classes
   - primary button classes
   - floating action button (FAB) sizing + positioning + z-index
2) Output these as a short “Reference Style Snapshot” (exact class strings if possible).

PHASE 1 — AUDIT (NO CHANGES YET)
Audit these pages (and only these; do not expand scope):
- /tasting (hub/lobby)
- /tasting/sessions
- /tasting/calendar (verify still matches reference)
- /my/journal
- /my/collection
- /my/wishlist
- /discover
- /discover/distilleries
- /discover/community
- /profile/help
Exclude: /profile (main profile form page), admin pages, and routes outside Layout (landing, join, naked, watch, etc).

For each audited page, check and report deviations vs the reference snapshot:
A) container width + px padding
B) header block spacing + icon size + H1/subline classes
C) cards (radius/shadow/padding/gaps)
D) tabs/segmented height + padding + active style
E) primary/secondary button height + radius + icon spacing
F) FAB consistency (if present)
G) empty state styling

Output a compact table-like list of deviations per page (bullet list is fine). Then STOP and proceed automatically to Phase 2 (no waiting for my “GO”).

PHASE 2 — MINIMAL PATCH (CHANGES ALLOWED)
Apply only minimal patches to fix deviations, prioritizing:
1) Container + header consistency (highest impact)
2) Tabs/buttons sizing consistency
3) Card padding + gaps
4) FAB positioning (only if inconsistent)

Constraints:
- Prefer editing local className strings in the page files.
- If a page is already using PageLayout but differs, align its props/classes to match reference.
- If a page is not using PageLayout, do NOT refactor heavily—wrap content with the smallest existing layout wrapper already used elsewhere (if available) OR apply minimal container classes directly.
- Do not change the semantic structure unless unavoidable (e.g., add one wrapper div).

PHASE 3 — VERIFICATION (MANDATORY)
After patches, you MUST:
1) run typecheck/build (or the repo’s standard build command)
2) confirm no route breaks
3) list all files changed with a 1-line explanation each
4) provide a checklist confirming that A–G are now consistent across all audited pages
5) explicitly confirm /profile was not forced into the wide layout
6) confirm dark mode not broken (basic class sanity check)

OUTPUT FORMAT (STRICT):
- “Reference Style Snapshot”
- “Audit Findings”
- “Patches Applied”
- “Verification Results”
- “Files Changed”
- “Final Consistency Checklist (A–G)”

DO IT NOW.