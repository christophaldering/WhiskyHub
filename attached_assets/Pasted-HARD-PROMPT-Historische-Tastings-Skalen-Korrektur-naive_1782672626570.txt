HARD PROMPT — Historische Tastings: Skalen-Korrektur (naives ×10 → verdichtete Kurve)

KONTEXT (nur ausführen, nicht umdeuten): Die Spalten normalized_nose/taste/finish/total in
historical_tasting_entries wurden beim Import naiv mit ×10 berechnet und liegen dadurch zu tief.
Sie werden neu aus den ERHALTENEN Original-Scores (nose_score/taste_score/finish_score/total_score,
Skala 0–10) über eine verdichtete Kurve berechnet. Reine Datenbank-Operation an EINER Tabelle.

ABSOLUTE VERBOTE (gelten für den GESAMTEN Lauf):
- KEIN git add / commit / push. KEINE Code-Datei ändern. NICHT shared/schema.ts anfassen.
- KEIN drizzle-kit (weder push noch generate) — das würde die Backup-Tabelle als Drift droppen.
- NUR die unten stehenden SQL-Statements gegen die DB ausführen, sonst nichts.
- Backup-Tabelle NICHT droppen. Nach der Verifikation STOPPEN und die Ausgaben berichten.
- Bei JEDER Abweichung von den Erwartungswerten: sofort STOPP, nicht weiterlaufen, nicht
  eigenmächtig "reparieren", nur berichten.

────────────────────────────────────────
SCHRITT 0 — PRE-CHECK (read-only, zuerst bestätigen)

  SELECT count(*) AS total,
         count(total_score) AS have_total,
         min(total_score) AS min_s, max(total_score) AS max_s,
         count(*) FILTER (WHERE total_score IS NULL AND normalized_total IS NOT NULL) AS only_normalized
  FROM historical_tasting_entries;

Erwartet: total=384, have_total=320, min_s≈3.53, max_s≈9.50, only_normalized=0.
Wenn only_normalized > 0 ODER total ≠ 384: STOPP + berichten, NICHT fortfahren.

────────────────────────────────────────
SCHRITT 1 — BACKUP-TABELLE (präziser Rollback)

  CREATE TABLE historical_tasting_entries_bak_20260628 AS
  SELECT id, nose_score, taste_score, finish_score, total_score,
         normalized_nose, normalized_taste, normalized_finish, normalized_total
  FROM historical_tasting_entries;

Erwartet: 384 Zeilen kopiert. Wenn ≠ 384: STOPP.

────────────────────────────────────────
SCHRITT 2 — UPDATE (Kurve ohne Boden; Slope 2,5/Pkt läuft unter 4 weiter; 1 Dezimale)

  UPDATE historical_tasting_entries SET
    normalized_nose = round((CASE WHEN nose_score IS NULL THEN NULL
      WHEN nose_score <= 7 THEN 75 + (nose_score - 4) * 2.5
      WHEN nose_score <= 9 THEN 82.5 + (nose_score - 7) * 5
      WHEN nose_score <= 10 THEN 92.5 + (nose_score - 9) * 2.5
      ELSE 95 END)::numeric, 1),
    normalized_taste = round((CASE WHEN taste_score IS NULL THEN NULL
      WHEN taste_score <= 7 THEN 75 + (taste_score - 4) * 2.5
      WHEN taste_score <= 9 THEN 82.5 + (taste_score - 7) * 5
      WHEN taste_score <= 10 THEN 92.5 + (taste_score - 9) * 2.5
      ELSE 95 END)::numeric, 1),
    normalized_finish = round((CASE WHEN finish_score IS NULL THEN NULL
      WHEN finish_score <= 7 THEN 75 + (finish_score - 4) * 2.5
      WHEN finish_score <= 9 THEN 82.5 + (finish_score - 7) * 5
      WHEN finish_score <= 10 THEN 92.5 + (finish_score - 9) * 2.5
      ELSE 95 END)::numeric, 1),
    normalized_total = round((CASE WHEN total_score IS NULL THEN NULL
      WHEN total_score <= 7 THEN 75 + (total_score - 4) * 2.5
      WHEN total_score <= 9 THEN 82.5 + (total_score - 7) * 5
      WHEN total_score <= 10 THEN 92.5 + (total_score - 9) * 2.5
      ELSE 95 END)::numeric, 1),
    updated_at = now();

Erwartet: UPDATE 384.

────────────────────────────────────────
SCHRITT 3 — VERIFIKATION (read-only)

3a) Anker + Ausreißer:
  SELECT total_score, normalized_total
  FROM historical_tasting_entries
  WHERE total_score IS NOT NULL
  ORDER BY total_score LIMIT 20;
  Erwartet u.a.: 3.53→73.8, 3.95→74.9, 4.00→75.0, danach steigend; 7→82.5, 9→92.5, 9.50→93.8.

3b) Band & Schnitt:
  SELECT min(normalized_total) AS min_n, max(normalized_total) AS max_n,
         round(avg(normalized_total)::numeric, 1) AS avg_n
  FROM historical_tasting_entries;
  Erwartet: min_n≈73.8, max_n≈93.8, avg_n deutlich höher als vor der Korrektur.

3c) Sicherheits-Check (muss überall 0 sein):
  SELECT
    count(*) FILTER (WHERE total_score  IS NOT NULL AND normalized_total  IS NULL) AS total_missing,
    count(*) FILTER (WHERE nose_score   IS NOT NULL AND normalized_nose   IS NULL) AS nose_missing,
    count(*) FILTER (WHERE taste_score  IS NOT NULL AND normalized_taste  IS NULL) AS taste_missing,
    count(*) FILTER (WHERE finish_score IS NOT NULL AND normalized_finish IS NULL) AS finish_missing
  FROM historical_tasting_entries;
  Erwartet: alle vier = 0.

────────────────────────────────────────
SCHRITT 4 — BERICHTEN & STOPPEN
Gib die Ausgaben von 3a, 3b, 3c zurück. Tabelle historical_tasting_entries_bak_20260628
STEHEN LASSEN. Nichts committen, nichts droppen, kein drizzle. Ende.

────────────────────────────────────────
ROLLBACK — NUR auf ausdrückliche Anweisung, falls Schritt 3 nicht stimmt:
  UPDATE historical_tasting_entries e SET
    normalized_nose = b.normalized_nose, normalized_taste = b.normalized_taste,
    normalized_finish = b.normalized_finish, normalized_total = b.normalized_total
  FROM historical_tasting_entries_bak_20260628 b
  WHERE e.id = b.id;
