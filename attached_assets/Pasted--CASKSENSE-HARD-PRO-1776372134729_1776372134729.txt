═══════════════════════════════════════════════════════════════════════════════
CASKSENSE — HARD PROMPT P2-B
POST-RATING SCREEN: "NÄCHSTER DRAFT" + "DELETE" VERSTECKEN
(Befunde #4, #18)
═══════════════════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 0 — PFLICHT-PRÜFUNG VOR JEDER ÄNDERUNG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Suche in client/src/labs/ nach der Komponente/Seite die nach einer
abgeschlossenen Dram-Bewertung angezeigt wird. Diese Seite zeigt:
  - Die Whisky-Karte mit Score
  - Buttons: "Edit" / "Nochmal verkosten" / "Delete"

Suche nach: "Nochmal verkosten", "nochmalVerkosten", "rerate", "re-rate",
"Edit", "Delete", "deleteRating", "deleteJournalEntry"

Notiere:
  1. Dateiname der Komponente
  2. Wie heißen die 3 Button-Komponenten exakt im JSX?
  3. Hat die Komponente bereits Zugriff auf eine Liste aller Journal-Einträge
     oder kann sie eine Query nach "drafts" / "isDraft=true" machen?
  4. Welche Route/URL hat diese Seite?

Berichte alle 4 Punkte BEVOR du etwas änderst.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 1 — "NÄCHSTER OFFENER DRAFT" BUTTON HINZUFÜGEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

NUR in der Datei die du in Schritt 0 identifiziert hast.

Füge eine Query hinzu die alle Journal-Einträge mit Status "draft" lädt:
  - Verwende die bereits existierende Query-Infrastruktur (TanStack Query)
  - Suche nach dem bestehenden API-Endpunkt für Journal-Einträge
    (wahrscheinlich /api/journal oder /api/participants/:id/journal)
  - Filter: nur Einträge mit isDraft=true ODER status="draft" ODER
    ohne finalisierten Overall-Score (je nachdem wie Drafts definiert sind)
  - Sortierung: nach updatedAt desc (neueste zuerst)

Füge einen neuen Button unter den bestehenden Buttons ein:

  Wenn drafts.length > 0 UND der aktuelle Eintrag NICHT selbst ein Draft ist:
    → Button: "Nächster offener Draft →" (goldener outlined Button)
    → onClick: navigiere zum ersten Draft in der Liste
      (Route: /labs/taste/drams/:id/rate oder die korrekte Rating-Route)

  Wenn drafts.length === 0:
    → Kein Button anzeigen (nicht anzeigen, nicht disabled)

  Wenn der aktuelle Eintrag selbst ein Draft ist:
    → Button nicht anzeigen

NICHT anfassen: die anderen Buttons, das Card-Layout, die Score-Anzeige.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 2 — "DELETE" BUTTON VERSTECKEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

In derselben Datei.

Den roten "Delete"-Button von der Hauptebene entfernen.

Option A (bevorzugt): Ein "···" Menü-Icon (drei Punkte) hinzufügen.
  Bei Klick: kleines Dropdown mit "Löschen" als einzige Option.
  Das Dropdown verwendet die bereits existierende onClick-Logik des
  Delete-Buttons unverändert.

Option B (falls A zu komplex): Delete-Button behalten aber:
  - Ans Ende verschieben (nach "Nochmal verkosten")
  - Outline-Stil statt Rot-Füllung
  - Kleinere Schrift

NICHT anfassen: die Delete-Logik selbst (API-Call, Mutation, Confirmation).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCHRITT 3 — i18n KEYS ERGÄNZEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

In client/src/lib/i18n.ts, NUR wenn diese Keys noch nicht existieren,
im bestehenden Block ergänzen (NICHT neuen Block erstellen):

EN: nextOpenDraft: "Next open Draft →"
DE: nextOpenDraft: "Nächster offener Draft →"

EN: allDramsRated: "All Drams rated ✓"
DE: allDramsRated: "Alle Drams bewertet ✓"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SICHERHEITSREGELN — NIEMALS ANFASSEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✗ server/ — keine einzige Datei
✗ shared/schema.ts
✗ package.json / package-lock.json
✗ .env / capacitor.config.ts
✗ Die Delete-Logik (API-Call, Confirmation-Dialog)
✗ Die "Nochmal verkosten" Logik
✗ Das Card-Layout und Score-Display
✗ Alle anderen Seiten

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFIKATION (nach Abschluss berichten)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ Post-Rating Screen: Button "Nächster offener Draft →" sichtbar (wenn Drafts vorhanden)
□ Button navigiert zum nächsten Draft
□ Wenn keine Drafts: kein Button sichtbar
□ Delete nicht mehr als roter Button auf Hauptebene
□ Delete-Funktion weiterhin erreichbar (über ··· oder verschoben)
□ Geänderte Datei(en): [Namen eintragen]
□ Draft-Query Endpunkt: [eintragen]
□ tsc --noEmit fehlerfrei