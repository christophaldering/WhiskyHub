HARD PROMPT — Boot-Datenkorrektur Historik (Scores-Kurve + ABV ×100), läuft auf Prod beim Deploy

KONTEXT: Korrektur als Boot-Code in server/index.ts, damit sie beim Deploy gegen die Produktions-DB
läuft (der Replit-Agent kann Prod nicht schreiben, der gebootete App-Prozess schon). Idempotent.
EINE Datei: server/index.ts. Zwei Edits.

VERBOTE: KEIN git commit/push/add. KEIN drizzle. KEINE Schema-Änderung. NUR server/index.ts,
NUR diese 2 SEARCH/REPLACE. Bei Anker-Abweichung: STOPP + melden.

ROLLBACK-TAG: git tag pre-boot-datafix-2026-06-28

──────── EDIT 1 — neue Funktion einfügen ────────
SUCHE (1×):
    log(`Backfill normalized scores failed: ${(e as Error).message}`, "seed");
  }
}

async function seedDistilleries() {

ERSETZE:
    log(`Backfill normalized scores failed: ${(e as Error).message}`, "seed");
  }
}

// Einmalige/idempotente Datenhygiene fuer historische Eintraege, laeuft bei jedem Boot
// gegen die DB, gegen die die App gerade laeuft (beim Deploy = Produktion).
async function applyHistoricalScaleFixes() {
  try {
    const { db: dbInst } = await import("./db");
    const { sql: sqlTag } = await import("drizzle-orm");

    // (1) Scores: normalized_* aus den ERHALTENEN Original-Scores ueber die verdichtete Kurve
    // (Band ~75-95, Steigung unter 4 verlaengert; reproduziert 4=75 ... 7=82.5 ... 9=92.5 ... 10=95).
    // Idempotent: rechnet immer aus den unveraenderten *_score-Quellspalten.
    await dbInst.execute(
      sqlTag`UPDATE historical_tasting_entries SET
        normalized_nose = round((CASE WHEN nose_score IS NULL THEN NULL
          WHEN nose_score <= 7 THEN 75 + (nose_score - 4) * 2.5
          WHEN nose_score <= 9 THEN 82.5 + (nose_score - 7) * 5
          WHEN nose_score <= 10 THEN 92.5 + (nose_score - 9) * 2.5
          ELSE 95 END)::numeric, 1),
        normalized_taste = round((CASE WHEN taste_score IS NULL THEN NULL
          WHEN taste_score <= 7 THEN 75 + (taste_score - 4) * 2.5
          WHEN taste_score <= 9 THEN 82.5 + (taste_score - 7) * 5
          WHEN taste_score <= 10 THEN 92.5 + (taste_score - 9) * 2.5
          ELSE 95 END)::numeric, 1),
        normalized_finish = round((CASE WHEN finish_score IS NULL THEN NULL
          WHEN finish_score <= 7 THEN 75 + (finish_score - 4) * 2.5
          WHEN finish_score <= 9 THEN 82.5 + (finish_score - 7) * 5
          WHEN finish_score <= 10 THEN 92.5 + (finish_score - 9) * 2.5
          ELSE 95 END)::numeric, 1),
        normalized_total = round((CASE WHEN total_score IS NULL THEN NULL
          WHEN total_score <= 7 THEN 75 + (total_score - 4) * 2.5
          WHEN total_score <= 9 THEN 82.5 + (total_score - 7) * 5
          WHEN total_score <= 10 THEN 92.5 + (total_score - 9) * 2.5
          ELSE 95 END)::numeric, 1)`
    );

    // (2) ABV: faelschlich als Anteil importierte Werte (<1) auf Prozent (x100).
    // Idempotent: nach der Korrektur ist nichts mehr < 1.
    const abvRes = await dbInst.execute(
      sqlTag`UPDATE historical_tasting_entries
             SET normalized_abv = round((normalized_abv * 100)::numeric, 1)
             WHERE normalized_abv IS NOT NULL AND normalized_abv < 1`
    );
    const abvCount = (abvRes as any)?.rowCount ?? 0;
    if (abvCount > 0) {
      log(`Datafix: corrected ${abvCount} historical fraction ABVs (x100)`, "seed");
    }
  } catch (e) {
    log(`Historical scale fixes failed: ${(e as Error).message}`, "seed");
  }
}

async function seedDistilleries() {

──────── EDIT 2 — Aufruf nach Backfill anhängen ────────
SUCHE (1×):
        backfillNormalizedScores().catch((e) =>
          log(`Backfill error: ${(e as Error).message}`, "seed"),
        ),

ERSETZE:
        backfillNormalizedScores()
          .catch((e) => log(`Backfill error: ${(e as Error).message}`, "seed"))
          .finally(() =>
            applyHistoricalScaleFixes().catch((e) =>
              log(`Datafix error: ${(e as Error).message}`, "seed"),
            ),
          ),

──────── VERIFIKATION ────────
  grep -c 'applyHistoricalScaleFixes' server/index.ts   # erwartet 2 (Definition + Aufruf)
server/index.ts muss ohne TS-/Build-Fehler kompilieren. Wenn grün: "fertig". KEIN commit/push.
