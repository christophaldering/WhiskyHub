HARD PROMPT — Cooper-Verkostungsnotiz: Treue vor Kürze (deine Worte bleiben drin)

ZIEL: Die generierte Prosa-Notiz dampft alles auf 1–4 Sätze je Dimension ein und wirft damit
viel von dem weg, was der Taster wirklich gesagt hat. Fix: Vollständigkeit/Treue wird oberste
Pflicht, die Abschnittslänge skaliert mit dem Gesagten (keine Höchstlänge), Token-Budget hoch.
Vier-Überschriften-Struktur (Nase/Gaumen/Abgang/Gesamteindruck) und "erfinde nichts" bleiben.
EINE Datei: server/routes.ts (die proseSystem-Prompt + der zugehörige Aufruf). KEIN Schema,
KEIN drizzle, KEINE Migration.

ROLLBACK-TAG ZUERST:
  git tag pre-prose-fidelity-2026-06-26

STEP 0 — DIAGNOSE (nur lesen). Bestätige per Ausgabe:
  - server/routes.ts enthält den Block `if (prose) {` mit `const proseSystem = ...` (~Z.5952).
  - Darin die Zeile mit "ein kurzer, evokativer Fließtext (1–4 Sätze)" und die Regel-Zeile
    "- Nimm ALLES auf, was er beschrieben hat — auch Nebennotizen, auch Verworfenes. Glätte nicht."
  - Der Aufruf direkt darunter hat `max_completion_tokens: 1500,` mit `content: proseSystem`.
  Zeig mir die drei Stellen + PLAN, dann STOPP und warte auf mein "OK".

NACH MEINEM OK — genau diese 3 Edits (alle im proseSystem-Block):

== EDIT 1: Längen-Deckel raus, Länge skaliert mit dem Gesagten ==
SUCHE (exakt):
Schreibe die vier Überschriften GENAU so (ein Wort je eigener Zeile, ohne Doppelpunkt, ohne Nummerierung, ohne Sternchen). Unter jede Überschrift kommt ein kurzer, evokativer Fließtext (1–4 Sätze) zu dieser Dimension; trenne die Abschnitte durch eine Leerzeile.
ERSETZE:
Schreibe die vier Überschriften GENAU so (ein Wort je eigener Zeile, ohne Doppelpunkt, ohne Nummerierung, ohne Sternchen). Unter jede Überschrift kommt zusammenhängender, evokativer Fließtext in seiner Stimme; trenne die Abschnitte durch eine Leerzeile. Die LÄNGE jedes Abschnitts richtet sich allein danach, wie viel der Taster zu dieser Dimension gesagt hat: hat er viel, differenziert und ausführlich erklärt, wird der Abschnitt entsprechend lang und nimmt all das auf; war er knapp, bleibt der Abschnitt knapp. Es gibt KEINE Höchstlänge — Kürze ist nie ein Ziel, Treue zum Gesagten schon.

== EDIT 2: Vollständigkeit zur obersten Pflicht machen ==
SUCHE (exakt):
- Nimm ALLES auf, was er beschrieben hat — auch Nebennotizen, auch Verworfenes. Glätte nicht.
ERSETZE:
- TREUE UND VOLLSTÄNDIGKEIT sind deine oberste Pflicht, wichtiger als Kürze oder Eleganz: JEDE eigenständige Beobachtung, Nuance, jeder Vergleich, jede Differenzierung, jeder Vorbehalt und jeder Nebengedanke des Tasters muss in der Notiz wiederzufinden sein — auch Nebennotizen, auch Verworfenes. Wirf nichts weg, weil es nebensächlich, umständlich oder weitschweifig klingt; wenn er es gesagt hat, gehört es hinein. Glätte nicht und raffe nicht zusammen — lieber ein Satz mehr, der eine seiner Wahrnehmungen bewahrt, als ein geglätteter, der sie verliert. Weglassen darfst du NUR reines Gesprächs-Beiwerk (Begrüßung, Fülllaute wie "ähm", Coopers eigene Fragen und Einwürfe) — niemals etwas Inhaltliches von ihm.

== EDIT 3: Token-Budget hoch (1500 -> 2400) ==
SUCHE (exakt):
          max_completion_tokens: 1500,
          reasoning_effort: "minimal",
          messages: [{ role: "system", content: proseSystem },
ERSETZE:
          max_completion_tokens: 2400,
          reasoning_effort: "minimal",
          messages: [{ role: "system", content: proseSystem },

VERIFIKATION (nur Ausgabe, NICHT committen/pushen):
  grep -c "KEINE Höchstlänge" server/routes.ts                                      # 1
  grep -c "TREUE UND VOLLSTÄNDIGKEIT sind deine oberste Pflicht" server/routes.ts    # 1
  grep -c "evokativer Fließtext (1–4 Sätze)" server/routes.ts                        # 0
  grep -c "auch Nebennotizen, auch Verworfenes. Glätte nicht." server/routes.ts      # 0
  grep -c "max_completion_tokens: 2400" server/routes.ts                             # >= 1
  Danach: tsc/Build-Check, dass server/routes.ts fehlerfrei kompiliert.

COMMIT-VERBOT (nochmal): NICHT git add, NICHT git commit, NICHT git push, NICHT drizzle,
KEINE Schema-Änderung. Nur die Datei ändern + Verifikations-Ausgabe zeigen. Push mache ich selbst.
