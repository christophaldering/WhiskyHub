Aufgabe: Cooper-Sprech-Bühne (verbundene Voice-Phase) zur immersiven Glimmer-Bühne machen

Kontext: In client/src/labs/components/rating/ImpressionCapture.tsx, Zweig "phase === voice && voice.status === connected" (aktuell ~Z. 363–414). Vier koordinierte Änderungen: (1) immersives Vollbild-Overlay ohne App-Navigation, (2) Cooper ~1/3 größer, (3) Glut pulst im Takt von Coopers Stimme (levelRef, mit speaking-Fallback für iOS), (4) Sternbild standardmäßig aus, zuschaltbar über den Mitschrift-Schalter, (5) Abschluss-Label "Ich bin fertig" → "Das war's für mich". Der NICHT-verbundene Voice-Intro-Zweig (mit CooperIntro) und alle anderen Phasen bleiben unberührt. Diese Aufgabe ist unabhängig vom Cooper-Intro-Prompt (anderer Codebereich, kein Konflikt).

WICHTIGE SICHERHEITSREGELN — gelten durchgehend:
- KEINE Commits. KEIN git push. KEIN drizzle-kit. KEINE Schema-Änderungen, KEINE Migrationen, KEINE .gitignore-Änderung. Reiner Frontend-/CSS-/i18n-Change.
- Falls du meinst, an Schema/DB/Migrationen ändern zu müssen: STOPP und melde dich.

Schritt 0 — Diagnose (NUR LESEN, dann auf mein "OK" warten):
1. git tag pre-glimmer-stage-2026-06-23 setzen (kein Commit).
2. Zeige client/src/labs/components/rating/ImpressionCapture.tsx Z. 360–414.
3. Zeige client/src/labs/components/rating/CooperBarrel.tsx Z. 18–40 (Props + svg-Wurzel mit className).
4. Zeige client/src/labs/labs-theme.css Z. 2966–2980 (cooper-barrel-Regeln).
5. grep -n "return {" client/src/labs/components/rating/useCooperVoice.ts → bestätige, dass levelRef UND speaking exportiert werden.
6. Bestätige, welche CSS-Variable die Seiten-Hintergrundfarbe ist: grep -n "\-\-labs-bg\|--labs-surface" client/src/labs/labs-theme.css | head. Nimm für das Overlay die Seiten-Hintergrund-Variable (bevorzugt var(--labs-bg); falls nicht vorhanden, var(--labs-surface)).
7. grep -n "voiceFinish" client/src/lib/i18n.ts → zeige den EN-Eintrag (falls vorhanden).

Plan (erst nach OK):

A) CooperBarrel.tsx — neue Betriebsart "live":
- Prop ergänzen: live?: boolean; // true = Glut folgt --cooper-level (Stimm-Takt)
- In der className-Zeile zusätzlich die Klasse "is-live" setzen, wenn live true ist (parallel zur bestehenden is-glowing-Logik). Beispiel: `cooper-barrel${glow ? " is-glowing" : ""}${live ? " is-live" : ""}${className ? ...}`.
- Sonst nichts an der SVG-Struktur ändern.

B) labs-theme.css — nach dem bestehenden @media-Block der cooper-barrel-Regeln (nach Z. ~2979) ergänzen:
.cooper-barrel.is-live .cooper-barrel-glow {
  animation: none;
  opacity: calc(0.42 + 0.58 * var(--cooper-level, 0));
  transform-box: fill-box;
  transform-origin: center;
  transform: scale(calc(1 + 0.10 * var(--cooper-level, 0)));
  transition: opacity 90ms linear, transform 90ms linear;
}
@media (prefers-reduced-motion: reduce) {
  .cooper-barrel.is-live .cooper-barrel-glow { transform: none; transition: opacity 120ms linear; }
}
(Opazität ist das robuste Hauptsignal; die Skalierung ist dezente Zugabe.)

C) ImpressionCapture.tsx — verbundene Phase:
C1) Refs am Komponentenanfang ergänzen (bei den anderen useRef):
    const glowWrapRef = useRef<HTMLDivElement | null>(null);
    const speakingRef = useRef(false);
    Direkt darunter (im Render-Body, vor return): speakingRef.current = voice.speaking;
C2) Neuer Effekt, der die Glut speist (rAF-Schleife, kein Re-Render pro Frame):
    useEffect(() => {
      if (!(phase === "voice" && voice.status === "connected")) return;
      let raf = 0; const t0 = performance.now();
      const loop = (now: number) => {
        const el = glowWrapRef.current;
        if (el) {
          let lvl = voice.levelRef?.current ?? 0;
          if (lvl < 0.02 && speakingRef.current) {
            const p = (now - t0) / 900;
            lvl = 0.32 + 0.22 * (0.5 + 0.5 * Math.sin(p * Math.PI * 2));
          }
          el.style.setProperty("--cooper-level", lvl.toFixed(3));
        }
        raf = requestAnimationFrame(loop);
      };
      raf = requestAnimationFrame(loop);
      return () => cancelAnimationFrame(raf);
    }, [phase, voice.status]);
C3) Den verbundenen Root-<div> (aktuell style minHeight "52vh") zu einem immersiven Vollbild-Overlay machen:
    style={{ position: "fixed", inset: 0, zIndex: 200, background: "var(--labs-bg)", display: "flex", flexDirection: "column", alignItems: "center", padding: "calc(env(safe-area-inset-top) + 24px) 20px calc(env(safe-area-inset-bottom) + 16px)", overflowY: "auto" }}
    (Falls --labs-bg laut Schritt 0 nicht existiert: var(--labs-surface).)
C4) Sternbild zuschaltbar: <LedgerConstellation data={voice.ledger as any} /> NUR rendern, wenn showTranscript true ist (an denselben Schalter koppeln wie die Mitschrift). Wenn ausgeblendet, KEIN Platzhalter.
C5) Cooper größer + live: <CooperBarrel size={132} glow /> ersetzen durch:
    <div ref={glowWrapRef} style={{ display: "flex" }}><CooperBarrel size={176} live /></div>
    (glow weglassen — im Live-Modus übernimmt is-live die Glut.)
C6) Abschluss-Label: t("v2.voiceFinish", "Ich bin fertig") → Inline-Default ändern zu "Das war's f\u00fcr mich". (Der Lade-Text "Einen Moment …" bleibt.)

D) i18n.ts — nur falls Schritt 0 einen EN-voiceFinish zeigt: dessen Wert auf "That's it for me" ändern. Kein neuer Key, keine DE-Einträge (DE läuft über den Inline-Default).

Verifikation (zeig mir die grep-Ausgaben):
1. grep -n "is-live\|live?" client/src/labs/components/rating/CooperBarrel.tsx → Prop + Klasse vorhanden.
2. grep -n "is-live\|--cooper-level" client/src/labs/labs-theme.css → neue Regeln vorhanden.
3. grep -n "cooper-level\|glowWrapRef\|size={176}\|live />\|Das war" client/src/labs/components/rating/ImpressionCapture.tsx → rAF-Var, Wrapper, Größe 176, live, neues Label.
4. grep -n "LedgerConstellation" client/src/labs/components/rating/ImpressionCapture.tsx → steht jetzt hinter showTranscript.
5. Typecheck (npm run check o.ä.) sauber, keine fehlenden Imports/Refs.
6. Commit-Verbot bestätigen: git log --oneline -3 und git status → NICHTS committet, NICHT gepusht; Tag pre-glimmer-stage-2026-06-23 existiert. Bestätige ausdrücklich: "Keine Commits, kein Push, keine Schema-/Migrationsänderung."

Rollback: git reset --hard pre-glimmer-stage-2026-06-23.
