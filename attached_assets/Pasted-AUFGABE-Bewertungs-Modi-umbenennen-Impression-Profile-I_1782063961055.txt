AUFGABE: Bewertungs-Modi umbenennen (Impression / Profile / In Depth) — nur i18n

KONTEXT
client/src/lib/i18n.ts enthält die Modus-Labels in zwei Sprachblöcken (EN ~Z.811, DE ~Z.10651). Sie sollen auf die finale Leiter umbenannt werden. Die interne Modus-Mechanik (quick/compact/guided/tisch) wird NICHT angefasst — nur die angezeigten Texte. Selektor und Chip ziehen die Werte automatisch.
Besonderheit: Der Key-Stamm "ratingTisch" trägt aktuell fälschlich "Impression"/"Eindruck" — das wird zu "Profile" korrigiert. "ratingQuick"-Keys fehlen und werden neu angelegt.

REGELN
- KEINE Commits, kein git push, kein drizzle-kit. Nur Datei-Änderung im Arbeitsbaum.
- Nur EINE Datei: client/src/lib/i18n.ts
- Brand-Begriffe "Impression", "Profile", "In Depth" sind in BEIDEN Sprachen englisch (identisch). Nur die Beschreibungen sind sprachabhängig.

================= STEP 0 — NUR DIAGNOSE =================
Im EN-Block (~Z.808-855) und im DE-Block (~Z.10648-10695) ausgeben und bestätigen:
1. Werte von ratingGuided, ratingGuidedD, ratingCompact, ratingCompactD, ratingTisch, ratingTischD in BEIDEN Blöcken.
2. Bestätige: ratingQuick / ratingQuickD / ratingQuickH existieren in KEINEM der beiden Blöcke (grep -n "ratingQuick" client/src/lib/i18n.ts → leer).
3. Nenne die exakte Zeile, vor der du im EN- bzw. DE-Block die neuen ratingQuick-Keys einfügen wirst (Vorschlag: direkt vor der jeweiligen "ratingGuided"-Zeile).
DANN WARTE auf mein OK.

================= AUSFÜHRUNG (nach OK) =================
Ändere im EN-Block die Werte (Key-basiert, Escapes der Datei beibehalten):
- ratingGuided  -> "In Depth · Step by step"
- ratingGuidedD -> "One dimension at a time — with aromas & notes."
- ratingCompact  -> "In Depth · All at once"
- ratingCompactD -> "All four dimensions at once — with aromas & notes."
- ratingTisch  -> "Profile"
- ratingTischD -> "The four dimensions, quick — no aromas."
Und füge im EN-Block (vor ratingGuided) NEU ein:
- ratingQuick: "Impression"
- ratingQuickD: "Just the overall first impression — two taps."
- ratingQuickH: "When time is short."

Ändere im DE-Block die Werte:
- ratingGuided  -> "In Depth · Schritt für Schritt"
- ratingGuidedD -> "Eine Dimension nach der anderen — mit Aromen & Notizen."
- ratingCompact  -> "In Depth · auf einmal"
- ratingCompactD -> "Alle vier Dimensionen auf einmal — mit Aromen & Notizen."
- ratingTisch  -> "Profile"
- ratingTischD -> "Die vier Dimensionen, schnell — ohne Aromen."
Und füge im DE-Block (vor ratingGuided) NEU ein:
- ratingQuick: "Impression"
- ratingQuickD: "Nur der erste Gesamteindruck — zwei Taps."
- ratingQuickH: "Wenn es schnell gehen soll."

Die Hint-Keys ratingGuidedH / ratingCompactH / ratingTischH bleiben unverändert.

================= VERIFIKATION =================
1. grep -nE "ratingQuick|ratingTisch:|ratingCompact:|ratingGuided:" client/src/lib/i18n.ts — zeige, dass beide Blöcke jetzt ratingQuick (="Impression"), ratingTisch (="Profile"), ratingCompact (="In Depth · …"), ratingGuided (="In Depth · …") haben.
2. Datei ist valides JS/TS (keine Syntaxfehler, Kommas korrekt).
3. KEIN Commit, kein git push, kein drizzle-kit. (Gilt ausdrücklich auch hier.)
4. git diff --stat (nur Anzeige).
