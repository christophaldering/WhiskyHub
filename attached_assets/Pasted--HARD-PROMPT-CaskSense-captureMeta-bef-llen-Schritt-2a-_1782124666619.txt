# HARD PROMPT — CaskSense: captureMeta befüllen, Schritt 2a (cooperTurns + cooperMode, Solo)

## Ziel
Das in Etappe 1 angelegte `captureMeta`-Feld erstmals befüllen: Beim Solo-Cooper-Eindruck die Gesprächstiefe (`cooperTurns`) und den Modus (`cooperMode`: schnell/neugierig/rabbithole) mitschreiben. Beides ist clientseitig im Eindruck-Dialog schon vorhanden — es ging bisher nur verloren. **Rein clientseitig, kein Server-Change, keine Schema-Änderung.**

## WICHTIG — Verhaltensregeln
- **SCHRITT 0 zuerst (NUR LESEN):** Anker bestätigen, kurze Bestätigung + Mini-Plan. **Dann WARTE auf mein OK.**
- **KEIN commit. KEIN git. KEINE Schema-/drizzle-Änderung.** Nur die drei genannten Dateien.

---

## SCHRITT 0 — Diagnose (nur lesen, dann warten)
- `client/src/labs/components/rating/impressionApi.ts`: bestätige in `ImpressionResult` die Zeilen `narrative?: string;` direkt gefolgt von `tookMs: number;` und `};`. Bestätige `export type Intensity = "schnell" | "neugierig" | "rabbithole";`.
- `client/src/labs/components/rating/ImpressionCapture.tsx`: bestätige die Zeile `onApply({ ...result, narrative: narrative.trim() || undefined });` und dass `transcript` und `intensity` im Component-Scope verfügbar sind.
- `client/src/labs/pages/LabsSolo.tsx`: bestätige `const narrativeRef = useRef<string>("");`, die zwei Reset-Stellen `rawImpressionRef.current = "";` + `narrativeRef.current = "";`, die Apply-Stelle mit `result.rawImpression`/`result.narrative`, und in `buildJournalBody` die Zeile mit `tastingNarrative: narrativeRef.current`.

**Wenn alles passt: kurze Bestätigung + Plan und auf mein OK warten.**

---

## SCHRITT 1 — `impressionApi.ts`: captureMeta in ImpressionResult
>>> ALT >>>
```
  narrative?: string;
  tookMs: number;
};
```
<<< ENDE ALT <<<
>>> NEU >>>
```
  narrative?: string;
  captureMeta?: { cooperTurns?: number; cooperMode?: string; entryModeUsed?: string; [key: string]: unknown };
  tookMs: number;
};
```
<<< ENDE NEU <<<

---

## SCHRITT 2 — `ImpressionCapture.tsx`: captureMeta beim Apply mitgeben
>>> ALT >>>
```
    onApply({ ...result, narrative: narrative.trim() || undefined });
```
<<< ENDE ALT <<<
>>> NEU >>>
```
    onApply({ ...result, narrative: narrative.trim() || undefined, captureMeta: { cooperTurns: transcript.filter((x) => x.role === "taster").length, cooperMode: intensity } });
```
<<< ENDE NEU <<<

---

## SCHRITT 3 — `LabsSolo.tsx` (vier Teil-Edits)

(a) Ref-Deklaration:
>>> ALT >>>
```
  const narrativeRef = useRef<string>("");
```
<<< ENDE ALT <<<
>>> NEU >>>
```
  const narrativeRef = useRef<string>("");
  const captureMetaRef = useRef<Record<string, unknown> | null>(null);
```
<<< ENDE NEU <<<

(b) handleImpressionApply (die Stelle mit `result.rawImpression`/`result.narrative`):
>>> ALT >>>
```
    rawImpressionRef.current = result.rawImpression || "";
    narrativeRef.current = result.narrative || "";
```
<<< ENDE ALT <<<
>>> NEU >>>
```
    rawImpressionRef.current = result.rawImpression || "";
    narrativeRef.current = result.narrative || "";
    captureMetaRef.current = result.captureMeta || null;
```
<<< ENDE NEU <<<

(c) Beide Reset-Stellen — **dieser Block kommt ZWEIMAL vor, bitte BEIDE ersetzen**:
>>> ALT >>>
```
    rawImpressionRef.current = "";
    narrativeRef.current = "";
```
<<< ENDE ALT <<<
>>> NEU >>>
```
    rawImpressionRef.current = "";
    narrativeRef.current = "";
    captureMetaRef.current = null;
```
<<< ENDE NEU <<<

(d) buildJournalBody:
>>> ALT >>>
```
      ...(narrativeRef.current ? { tastingNarrative: narrativeRef.current } : {}),
```
<<< ENDE ALT <<<
>>> NEU >>>
```
      ...(narrativeRef.current ? { tastingNarrative: narrativeRef.current } : {}),
      ...(captureMetaRef.current ? { captureMeta: captureMetaRef.current } : {}),
```
<<< ENDE NEU <<<

---

## VERIFIKATION
- `impressionApi.ts`: `ImpressionResult` enthält jetzt `captureMeta?`.
- `ImpressionCapture.tsx`: `onApply` übergibt `captureMeta` mit `cooperTurns` + `cooperMode`.
- `LabsSolo.tsx`: `captureMetaRef` deklariert; in `handleImpressionApply` gesetzt; an **beiden** Reset-Stellen genullt; in `buildJournalBody` konditional angehängt.
- TypeScript bleibt sauber.
- **Nochmals: KEIN commit, KEIN git, KEINE Schema-/drizzle-Änderung. Drei editierte Dateien.**
