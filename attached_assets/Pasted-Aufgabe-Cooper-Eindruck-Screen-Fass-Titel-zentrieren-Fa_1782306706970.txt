Aufgabe: Cooper-Eindruck-Screen — Fass + Titel zentrieren, Fass 50% größer, Glut deutlich glimmen lassen.

SICHERHEITSREGELN: KEINE Commits, KEIN Push, KEIN drizzle-kit, KEINE Schema-Änderung.

git tag pre-barrel-glow-2026-06-24 setzen. Dann drei Edits:

=== A) client/src/labs/components/rating/ImpressionCapture.tsx — Kopf zentrieren + Fass größer. Ersetze EXAKT:
      <div style={{ display: "flex", flexDirection: "column", alignItems: "flex-start", gap: SP.xs, marginBottom: SP.xs }}>
        <span style={{ flexShrink: 0, display: "flex" }}><CooperBarrel size={44} glow /></span>
        <div style={{ fontFamily: FONT.display, fontSize: 22, color: LABS_THEME.text }}>
          {t("v2.impressionTitle", "Was nimmst du wahr?")}
        </div>
      </div>
durch:
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: SP.xs, marginBottom: SP.sm }}>
        <span style={{ flexShrink: 0, display: "flex" }}><CooperBarrel size={66} glow /></span>
        <div style={{ fontFamily: FONT.display, fontSize: 22, color: LABS_THEME.text, textAlign: "center" }}>
          {t("v2.impressionTitle", "Was nimmst du wahr?")}
        </div>
      </div>
(Falls SP.sm nicht existiert: SP.sm durch SP.xs ersetzen.)

=== B) client/src/labs/labs-theme.css — Glut deutlicher (pulsiert in Helligkeit + Größe). Ersetze EXAKT:
.cooper-barrel.is-glowing .cooper-barrel-glow {
  animation: cooper-glow-breathe 3.4s ease-in-out infinite;
}
@keyframes cooper-glow-breathe {
  0%, 100% { opacity: 0.72; }
  50%      { opacity: 1; }
}
durch:
.cooper-barrel.is-glowing .cooper-barrel-glow {
  transform-box: fill-box;
  transform-origin: center;
  animation: cooper-glow-breathe 3.2s ease-in-out infinite;
}
@keyframes cooper-glow-breathe {
  0%, 100% { opacity: 0.82; transform: scale(0.96); }
  50%      { opacity: 1; transform: scale(1.10); }
}

=== C) client/src/labs/components/rating/CooperBarrel.tsx — Halo etwas voller. Ersetze EXAKT:
          <feGaussianBlur stdDeviation="2.4" />
durch:
          <feGaussianBlur stdDeviation="3.2" />

Verifikation:
1. grep -n "alignItems: \"center\"\|size={66}\|textAlign: \"center\"" client/src/labs/components/rating/ImpressionCapture.tsx
2. grep -n "transform: scale(1.10)\|opacity: 0.82" client/src/labs/labs-theme.css
3. grep -n "stdDeviation=\"3.2\"" client/src/labs/components/rating/CooperBarrel.tsx
4. Typecheck sauber.
5. git status → nichts committet/gepusht. Bestätige: "Keine Commits, kein Push, keine Schema-Änderung."

Rollback: git reset --hard pre-barrel-glow-2026-06-24.
