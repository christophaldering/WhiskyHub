KONTEXT: CaskSense (WhiskyHub). Konsolidierung „Empfehlungen": die Empfehlungs-Features wandern aus der Whisky-DNA in ihr eigenes Zuhause (KI-Seite „Empfehlungen"); das Analyse-Profil wird reine Analyse. Setzt auf dem bereits angewendeten Profil-Merge auf. ERSETZT den früheren „Empfehlungen im Profil ausblenden"-Prompt — diesen NICHT mehr anwenden.

SICHERHEIT: Rollback-Tag: git tag pre-recs-consolidation-2026-06-28
KEINE Commits, KEINE Pushes, KEIN drizzle/Schema, KEIN „Published".

STEP 0 — DIAGNOSE:
- Prüfe, dass die SUCHEN-Anker (Dateien B, C, D3–D5) je GENAU 1× vorkommen.
- Für die zwei LÖSCHUNGEN in LabsWhiskyDNA.tsx (D1, D2): lies den Bereich, bestätige die Block-Grenzen exakt, lösche nur den beschriebenen Block. Bei Unsicherheit: STOPP und melde.

=================================================================
DATEI A: NEU client/src/labs/pages/LabsTryNext.tsx
=================================================================
Lege die separat gelieferte Datei LabsTryNext.tsx unter client/src/labs/pages/ an (vollständiger Inhalt = die bereitgestellte Datei). Nichts daran ändern.

=================================================================
DATEI B: client/src/labs/pages/hubTiles.tsx — „Empfehlungen"-Kachel sichtbar machen
=================================================================
--- SUCHEN:
    testId: "labs-link-ai-insights-recommendations",
    hidden: true,
--- ERSETZEN:
    testId: "labs-link-ai-insights-recommendations",

=================================================================
DATEI C: client/src/labs/pages/LabsRecommendations.tsx — „Try next" einbinden
=================================================================
--- C1 SUCHEN:
import AuthGateMessage from "@/labs/components/AuthGateMessage";
--- C1 ERSETZEN:
import AuthGateMessage from "@/labs/components/AuthGateMessage";
import LabsTryNext from "./LabsTryNext";

--- C2 SUCHEN:
          </section>

          <section style={{ display: "flex", flexDirection: "column", gap: 12 }} data-testid="section-ai-recommendations">
--- C2 ERSETZEN:
          </section>

          <LabsTryNext />

          <section style={{ display: "flex", flexDirection: "column", gap: 12 }} data-testid="section-ai-recommendations">

=================================================================
DATEI D: client/src/labs/pages/LabsWhiskyDNA.tsx
=================================================================

D1 — LÖSCHEN: der „Try next"-Render-Block (zieht in LabsTryNext.tsx um).
Lösche den kompletten Block von der Zeile
          {/* Try next — recommendations to strengthen weak axes (data-based) */}
bis EINSCHLIESSLICH des `)}`, das die Bedingung `{recSource === "data" && …}` schließt (der Block mit data-testid="section-try-next"). Sein Ende sieht exakt so aus:
                })}
              </div>
            </div>
          )}
→ Danach muss "section-try-next" 0× vorkommen.

D2 — LÖSCHEN: die dadurch ungenutzte Wunschlisten-Maschinerie.
Lösche den zusammenhängenden Block von
  const { data: wishlistEntries } = useQuery<WishlistEntry[]>({
bis EINSCHLIESSLICH des `});`, das `const removeRecMutation = useMutation({ … })` schließt — d.h. wishlistEntries-Query, savedKeys, wishlistIdByKey, collectionKeys, die States justSavedKeys/savingKey/removingKey, saveRecMutation und removeRecMutation. STOPP direkt VOR `const labels = useMemo(`.
→ Danach müssen "saveRecMutation", "wishlistEntries" und "savedKeys" je 0× vorkommen.
WICHTIG: die `recs`-Query (const { data: recs … }) NICHT löschen — sie wird weiter vom Status-Text genutzt.

D3 — Quelle-Umschalter + Status im Profil ausblenden: ÖFFNEN
--- SUCHEN:
          {/* Recommendation source tiles — top of page so users don't miss the AI option */}
          <div
            className="labs-fade-in"
            style={{ marginBottom: 14 }}
            data-testid="section-rec-source-tiles"
--- ERSETZEN:
          {/* Recommendation source tiles (im Profil ausgeblendet — Empfehlungen leben unter KI) */}
          {!embedded && (
          <div
            className="labs-fade-in"
            style={{ marginBottom: 14 }}
            data-testid="section-rec-source-tiles"

D4 — dieselbe Leiste: SCHLIESSEN (vor dem Phase-Block)
--- SUCHEN:
          </div>

          {/* Phase badge + explanation */}
--- ERSETZEN:
          </div>
          )}

          {/* Phase badge + explanation */}

D5 — „KI-Vorschläge" im Profil ausblenden
--- SUCHEN:
          {recSource === "ai" && (
--- ERSETZEN:
          {!embedded && recSource === "ai" && (

=================================================================
VERIFIKATION:
- LabsTryNext.tsx existiert; LabsRecommendations rendert <LabsTryNext />.
- hubTiles: die rec-Kachel hat KEIN hidden:true mehr → KI zeigt jetzt „Empfehlungen".
- LabsWhiskyDNA: "section-try-next"=0, "saveRecMutation"=0, "wishlistEntries"=0; "recs" und "recSource" bleiben >0.
- Build/Typecheck EXIT 0.
- Klicktest (a): Analyse → „Dein Profil" → KEINE Empfehlungsquelle / kein „Try next" / keine „KI-Vorschläge" mehr.
- Klicktest (b): KI → „Empfehlungen" zeigt: statistisch + „Try next" + KI-Vorschläge.
ERINNERUNG: KEINE Commits, KEINE Pushes, KEIN drizzle/Schema, KEIN „Published".
