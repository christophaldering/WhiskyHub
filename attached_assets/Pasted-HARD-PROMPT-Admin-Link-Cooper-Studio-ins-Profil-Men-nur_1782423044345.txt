HARD PROMPT — Admin-Link "Cooper-Studio" ins Profil-Menü (nur für Admins, unter "Cooper")

KONTEXT: Das Cooper-Studio (Stimme/Modus/Persona-Regler) liegt unter /labs/voice-probe, ist aber nirgends verlinkt. Wir legen einen dezenten, NUR für Admins sichtbaren Eintrag "Cooper-Studio" direkt unter den bestehenden "Cooper"-Eintrag im Profil-Menü. Nur eine Datei: client/src/components/ProfileMenu.tsx. Reines Frontend, kein Schema/keine API-Änderung.

STEP 0 — DIAGNOSE (nichts ändern bis "go"):
Setze:  git tag pre-cooper-studio-link-2026-06-25
Bestätige (read-only): In client/src/components/ProfileMenu.tsx stehen die Label-Objekte mit `cooper: "Cooper", ...` auf zwei Zeilen (DE ~248, EN ~256). Der Cooper-Menüeintrag ist der Block `{isLabs ? (<MenuButton ... label={L.cooper} ... navigate("/labs/taste/cooper") ... testId="m2-profile-cooper" />) : null}`, gefolgt von einem Divider-`<div>`. Shield ist aus lucide-react importiert. Admin-Erkennung im File: `session.role === "admin"`. Melde kurz, dann WARTE auf "go".

STEP 1 — Labels ergänzen (DE + EN):
Ersetze  `cooper: "Cooper", secSettings: "Einstellungen", settings: "Einstellungen",`
durch    `cooper: "Cooper", cooperStudio: "Cooper-Studio", secSettings: "Einstellungen", settings: "Einstellungen",`
Ersetze  `cooper: "Cooper", secSettings: "Settings", settings: "Settings",`
durch    `cooper: "Cooper", cooperStudio: "Cooper-Studio", secSettings: "Settings", settings: "Settings",`

STEP 2 — Admin-Eintrag einfügen:
Ersetze den Block
      {isLabs ? (
        <MenuButton theme={tv}
          icon={<Sparkles style={{ width: 18, height: 18, color: tv.accent }} />}
          label={L.cooper}
          onClick={() => { onClose(); navigate("/labs/taste/cooper"); }}
          testId="m2-profile-cooper"
        />
      ) : null}

      <div style={{ height: 1, background: tv.border, margin: "8px 0" }} />
durch
      {isLabs ? (
        <MenuButton theme={tv}
          icon={<Sparkles style={{ width: 18, height: 18, color: tv.accent }} />}
          label={L.cooper}
          onClick={() => { onClose(); navigate("/labs/taste/cooper"); }}
          testId="m2-profile-cooper"
        />
      ) : null}
      {isLabs && session.role === "admin" ? (
        <MenuButton theme={tv}
          icon={<Shield style={{ width: 18, height: 18, color: tv.accent }} />}
          label={L.cooperStudio}
          onClick={() => { onClose(); navigate("/labs/voice-probe"); }}
          testId="m2-profile-cooper-studio"
        />
      ) : null}

      <div style={{ height: 1, background: tv.border, margin: "8px 0" }} />

VERIFIKATION (zeig mir):
  - grep -n "cooperStudio" client/src/components/ProfileMenu.tsx → 3 Treffer (DE-Label, EN-Label, label={L.cooperStudio}).
  - grep -n "m2-profile-cooper-studio\|/labs/voice-probe" client/src/components/ProfileMenu.tsx → der neue Eintrag vorhanden, gegated mit `isLabs && session.role === "admin"`.
  - Build/Typecheck grün.
  - Logik: Nicht-Admins sehen den Eintrag NICHT; als Admin im Labs-UI erscheint "Cooper-Studio" direkt unter "Cooper" und navigiert auf /labs/voice-probe.

NICHT TUN:
  - KEIN git commit, KEIN git push.
  - KEIN drizzle, KEINE Schema-/API-Änderung.
  - NUR client/src/components/ProfileMenu.tsx anfassen; den "Cooper"-Eintrag selbst nicht verändern.
Bei Unklarheit STOPP und frag.
Rollback: git reset --hard pre-cooper-studio-link-2026-06-25
