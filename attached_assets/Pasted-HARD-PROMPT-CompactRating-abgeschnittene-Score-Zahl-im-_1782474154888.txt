HARD PROMPT — CompactRating: abgeschnittene Ø-Score-Zahl im Header beheben

KONTEXT: In client/src/labs/components/rating/CompactRating.tsx ist der Kopf ein Flex-Row (space-between): links Whisky-Name/Region, rechts die große Ø-Score-Zahl. Der linke Block hat kein min-width:0, der rechte kein flex-shrink:0 → langer Name drückt den Score aus dem Container (abgeschnitten). Fix: links flexibel + umbrechbar, rechts unverkürzbar, plus gap. Eine Datei, reines Layout/CSS-in-JS.

STEP 0 — DIAGNOSE (nichts ändern bis "go"):
Bestätige (read-only): In CompactRating.tsx gibt es den Header
  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: SP.lg }}>
    <div>
      ...whisky.name... fontSize: 20 ...
    </div>
    <div style={{ textAlign: "right" }}>
mit der Score-Zahl (data-testid="compact-overall-avg") rechts. Melde kurz, dann WARTE auf "go".

STEP 1 — Header-Flex robust machen
Ersetze EXAKT
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: SP.lg }}>
        <div>
          {!whisky.blind && whisky.name && (
            <div style={{ fontFamily: FONT.display, fontSize: 20, fontWeight: 600, color: "var(--labs-text)" }}>
              {whisky.name}
            </div>
          )}
          {!whisky.blind && whisky.region && (
            <div style={{ fontSize: 13, color: "var(--labs-text-muted)", fontFamily: FONT.body }}>{whisky.region}</div>
          )}
        </div>
        <div style={{ textAlign: "right" }}>
durch
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: SP.sm, marginBottom: SP.lg }}>
        <div style={{ minWidth: 0, flex: "1 1 auto" }}>
          {!whisky.blind && whisky.name && (
            <div style={{ fontFamily: FONT.display, fontSize: 20, fontWeight: 600, color: "var(--labs-text)", overflowWrap: "anywhere" }}>
              {whisky.name}
            </div>
          )}
          {!whisky.blind && whisky.region && (
            <div style={{ fontSize: 13, color: "var(--labs-text-muted)", fontFamily: FONT.body }}>{whisky.region}</div>
          )}
        </div>
        <div style={{ textAlign: "right", flexShrink: 0 }}>

VERIFIKATION (zeig mir):
  - grep -c "minWidth: 0, flex: \"1 1 auto\"" client/src/labs/components/rating/CompactRating.tsx → 1
  - grep -c "textAlign: \"right\", flexShrink: 0" client/src/labs/components/rating/CompactRating.tsx → 1
  - grep -c "overflowWrap: \"anywhere\"" client/src/labs/components/rating/CompactRating.tsx → 1
  - Build grün. Die Ø-Zahl (z. B. „76.5/100") steht jetzt vollständig rechts; der lange Name bricht links um.

NICHT TUN: KEIN commit/push, KEIN drizzle/Schema, NUR diese eine Stelle. Bei nicht-exaktem Match: STOPP und frag.
Rollback: git reset --hard <letzter Checkpoint>
