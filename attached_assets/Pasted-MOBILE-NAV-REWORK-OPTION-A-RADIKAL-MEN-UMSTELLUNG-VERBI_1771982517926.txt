MOBILE NAV REWORK – OPTION A (RADIKAL) + MENÜ-UMSTELLUNG (VERBINDLICH)
NUR NAV/UI + LABELS/UMBENENNUNGEN. KEINE FUNKTIONSÄNDERUNG.

Ziel:
- Mobile Navigation bleibt “WhatsApp/Strava”-artig (BottomNav only, kein Hamburger).
- Zusätzlich: Menüstruktur & Labels wie unten beschrieben anpassen.
- Wichtig: Es darf KEINE Funktionalität entfernt/deaktiviert werden. Nur Zugänge/Labels/Platzierung.

ABSOLUTE VERBOTE (bei Unsicherheit: STOPPEN & berichten):
- keine Datei löschen
- keine Datei verschieben/umbenennen
- keine Route/URL ändern/entfernen/umleiten (Paths bleiben identisch)
- keine API/DB/Schema Änderungen
- keine State-/Hook-/Business-Logik Änderungen
- keine Rollen/Rechte Änderungen
- keine Features deaktivieren oder verstecken (alles muss irgendwo erreichbar bleiben)
- keine inhaltliche Textumformulierung außerhalb der explizit angeordneten Umbenennungen
- kein Refactoring “zur Vereinfachung”

ERLAUBT:
- Navigationseinträge neu anordnen (ohne Routeänderungen)
- sichtbare Namen/Labels ändern (laut Liste)
- Tabs/Segmented-Control Labels ändern
- UI-/Layout-Anpassungen (Premium Ruhe)
- Hamburger/Sidebar auf Mobile entfernen (Layout-bedingt), Desktop bleibt Sidebar

------------------------------------------------------------
A) MOBILE NAV – FINAL (BottomNav only)
------------------------------------------------------------

Mobile hat exakt 5 Tabs:
1) Home
2) Tastings
3) Tagebuch
4) Entdecken
5) Profil

MOBILE LABEL REGEL:
- Labels max. 10 Zeichen, einzeilig, kein Umbruch.
- Falls “Tastings” nicht passt: “Tasting”.
- Falls “Entdecken” nicht passt: “Explore”.
- “Tagebuch” bleibt, falls zu lang: “Journal”.

Hamburger-Menü auf Mobile vollständig entfernen (nicht nur verstecken).
Desktop Sidebar bleibt unverändert.

------------------------------------------------------------
B) MENÜ-UMSTELLUNG (VERBINDLICH, OHNE ROUTEÄNDERUNGEN)
------------------------------------------------------------

1) Tastings/Home wird neu aufgeteilt:

B1) Neue 1. Ebene: “Home”
- “Home” ist eine eigene Hauptnavigation (BottomNav Tab 1).
- “Home” darf KEINE Untermenüs haben.
- Inhalt von “Home” ist ausschließlich der bisherige “Lobby”-Bereich.
- Wichtig: Route bleibt unverändert; wenn die Lobby bisher /app ist, dann führt “Home” auf /app.

B2) “Tastings/Home” existiert zukünftig NICHT mehr als Menüpunkt.
- Der bisherige Punkt “Tastings > Home” wird aus der Tastings-Sektion entfernt.
- Nichts wird gelöscht – nur der Einstiegspunkt wandert zu “Home”.

B3) Innerhalb “Tastings” sollen vorerst nur folgende Unterpunkte (Tabs/Segmented) prominent sein:
- “Vorlagen”
- “Pairings”
(= in der Tastings-Sektion/Home-Ansicht, aber ohne Lobby)

2) Umhängen “Jetzt/Weiter”:
- Der bisherige Bereich “Jetzt” bzw. “Weiter” soll NICHT mehr als eigener Hauptpunkt erscheinen.
- “Jetzt/Weiter” wird ersetzt durch: “Tastings > Tastings”
Interpretation (verbindlich, ohne Routen zu ändern):
- Der Inhalt/Link, der bisher unter “Jetzt/Weiter” zur Fortsetzung/Aktiven Session führte, muss künftig unter “Tastings” erreichbar sein,
  als klarer Einstieg (z.B. erster Tab oder erste Karte) mit Label “Tastings”.
- Falls bisher ein “Continue”-Link existiert: er bleibt funktional, wird aber unter “Tastings” platziert.

3) “Tastings/Tasting-Kalender” umbenennen:
- Sichtbarer Name wird: “Kalender”
- Route bleibt wie sie ist (z.B. /calendar oder /tasting-calendar o.ä. NICHT ändern).
- Alle Verweise im UI müssen konsistent “Kalender” zeigen (Sidebar/BottomNav/Tab-Titel/Header).

4) Tagebuch Umbenennung:
- “Tagebuch > Mein privates Verkostungstagebuch” umbenennen in:
  “Private Verkostungen”
- Route bleibt unverändert.
- Alle sichtbaren UI-Stellen anpassen: Sidebar Item, Page Header Title, evtl. Breadcrumb/Tab Label.

WICHTIGER HINWEIS:
- Es geht um sichtbare Labels + Menüstruktur.
- KEINE funktionale Änderung, keine Logikänderung, keine Datenmodelländerung.

------------------------------------------------------------
C) UMSETZUNGSANWEISUNG (SCHRITT FÜR SCHRITT)
------------------------------------------------------------

Teil 1 – Bestandsaufnahme (kurz, aber vollständig):
1) Finde die Navigation-Definition(en) (Desktop Sidebar, Mobile BottomNav, Mobile Drawer).
2) Liste die betroffenen Menüpunkte inkl. ihrer Routes:
   - Lobby (/app)
   - Tastings Übersicht (/sessions o.ä.)
   - Vorlagen (/tasting-templates)
   - Pairings (/pairings)
   - Kalender (bisher “Tasting-Kalender”, Route unverändert)
   - Tagebuch privates Verkostungstagebuch (Route unverändert)
   - “Jetzt/Weiter” (Route/Action identifizieren)
3) Nenne kurz, wo die Labels herkommen (i18n Keys? hardcoded?).

Teil 2 – Umsetzung:
4) Mobile: Hamburger/Drawer entfernen, BottomNav-only Layout aktivieren.
5) BottomNav Tab 1 heißt “Home” und zeigt ausschließlich Lobby (/app).
6) Entferne “Tastings > Home” als Menüeintrag/Tab (nur den Einstiegspunkt).
7) In “Tastings” prominent nur “Vorlagen” und “Pairings” anzeigen (als Tabs/Segmented oder klare Buttons).
8) “Jetzt/Weiter” Einstieg in Tastings integrieren:
   - Der frühere Continue/Now Zugang muss im Tastings-Bereich erreichbar sein.
   - Label: “Tastings” (oder “Jetzt” falls zwingend nötig, aber Ziel ist “Tastings”).
9) “Tasting-Kalender” überall sichtbar umbenennen in “Kalender”.
10) “Mein privates Verkostungstagebuch” überall sichtbar umbenennen in “Private Verkostungen”.
11) Sicherstellen: Desktop Sidebar weiterhin funktioniert und konsistente Labels nutzt (außer wo anders gewünscht).

Teil 3 – QA / Selbstprüfung (Pflicht):
12) Explizit bestätigen:
   - keine Route geändert
   - keine Features entfernt/deaktiviert
   - keine Logik geändert
   - nur Navigation/Labels/UI angepasst
13) Checkliste Mobile:
   - BottomNav: Home | Tastings | Tagebuch | Entdecken | Profil
   - Kein Hamburger sichtbar
   - Home führt zur Lobby (/app)
   - Tastings enthält Vorlagen + Pairings und den früheren “Jetzt/Weiter” Einstieg
   - Kalender heißt “Kalender”
   - Tagebuch heißt “Private Verkostungen”
   - Labels umbrechen nicht unter Icons (einzeilig, truncate falls nötig)
14) Liste geänderter Dateien + Kurzbeschreibung pro Datei.

START:
Erst Teil 1 (Analyse), dann Teil 2 (Änderungen), dann Teil 3 (QA).
Keine Abkürzungen.