Aufgabe: Cooper-Voice — Zuhör-Signale statt Gequatsche, adaptiv gestaffelt + längere Schweige-Schwelle.

Ziel: Im Voice-Token-Endpoint (/api/voice-probe/token) bekommt Cooper eine klare, positive Gesprächsführung: häufigste Reaktion = knappes Zuhör-Signal ("Hm", "erzähl weiter") oder Schweigen; eigene Frage/Gedanke = seltene Ausnahme; Bewertung verboten. Häufigkeit pro Ebene gestaffelt (Connoisseur am stillsten → Beginner am meisten Signale + behutsame Orientierung), adaptiv im Auto-Modus. Schweige-Schwelle erhöht. NUR Voice (Text-Pfad bleibt unberührt, kommt separat).

SICHERHEITSREGELN: KEINE Commits, KEIN Push, KEIN drizzle-kit, KEINE Schema-Änderung. Nur server/routes.ts.

Schritt 0 — Diagnose (NUR LESEN, dann auf mein OK warten):
1. git tag pre-cooper-listen-2026-06-23 setzen.
2. server/routes.ts Z. 5613–5615 zeigen (rawLevel/cooperLevel/instructions-Selektor).
3. server/routes.ts Z. 5646 zeigen (silenceMs).
4. Bestätige: die vier Instruktions-Konstanten (connoisseur/expert/beginner/adaptive Instructions) werden NICHT verändert.

Plan (erst nach OK):

A) Selektor erweitern. Ersetze EXAKT:
ALT:
      const instructions = cooperLevel === "beginner" ? beginnerInstructions : cooperLevel === "expert" ? expertInstructions : cooperLevel === "connoisseur" ? connoisseurInstructions : adaptiveInstructions;
NEU:
      const baseInstr = cooperLevel === "beginner" ? beginnerInstructions : cooperLevel === "expert" ? expertInstructions : cooperLevel === "connoisseur" ? connoisseurInstructions : adaptiveInstructions;
      const continuerBlock = "So führst du das Gespräch — das ist dein wichtigstes Werkzeug: Du empfängst, du lenkst nicht. Deine häufigste hörbare Reaktion ist ein knappes Zuhör-Signal, das den Faden sofort zurückgibt — ein 'Hm', 'ich höre', 'dem würde ich nachgehen', 'bleib da mal', 'erzähl weiter'. Diese Signale sagen 'sprich weiter', nicht 'beantworte mir das'. Sie bewerten NIE, was er gesagt hat: 'schön', 'toll', 'genau', 'interessant', 'spannend' sind verboten — sie zeigen rückwärts auf seine Worte; deine Signale zeigen vorwärts auf seinen nächsten Gedanken. Eine eigene Frage oder ein eigener Gedanke ist die seltene Ausnahme, nicht die Regel — nur wenn ein einziger Faden es wirklich wert ist, und dann ein Halbsatz. Oft ist die beste Antwort, einfach zu schweigen und ihm Raum zu lassen.";
      const continuerByLevel: Record<string, string> = {
        connoisseur: "Bei einem Kenner bist du am stillsten — meist genügt reines Schweigen; ein Zuhör-Signal nur sparsam, eine eigene Regung höchst selten.",
        expert: "Etwas öfter ein knappes Signal, aber überwiegend still; eigene Anstöße bleiben selten.",
        beginner: "Häufiger ein warmes Signal, das zum Weitersprechen ermutigt; gerät er ins Stocken, orientiere behutsam mit einem einfachen Entweder-oder als allgemeinem Anker (nie über diese Flasche). Auch hier: zum Weiterdenken ermutigen, nicht loben.",
        adaptive: "Wie oft du ein Signal gibst, richtest du nach dem Taster — wirkt er sicher und flüssig, zieh dich fast ganz ins Schweigen zurück; wirkt er unsicher oder wortsuchend, gib häufiger ein warmes 'erzähl weiter' und orientiere behutsam, wenn er feststeckt.";
      };
      const instructions = baseInstr + "\n\n" + continuerBlock + " " + (continuerByLevel[cooperLevel] || continuerByLevel.adaptive);

B) Schweige-Schwelle erhöhen. Ersetze EXAKT:
ALT:
      const silenceMs = mode === "tiefsinnig" ? 1100 : 900;
NEU:
      const silenceMs = mode === "tiefsinnig" ? 1400 : 1100;

ACHTUNG: In continuerByLevel die adaptive-Zeile mit Komma abschließen (nicht Strichpunkt) — gültiges Objektliteral. Bitte nach dem Einfügen den Block auf korrekte Klammern/Kommas prüfen.

Verifikation (zeig grep-Ausgaben):
1. grep -n "continuerBlock\|continuerByLevel\|baseInstr" server/routes.ts → Definitionen + Verwendung.
2. grep -n "silenceMs = mode" server/routes.ts → 1400 : 1100.
3. Bestätige, dass die vier Instruktions-Konstanten unverändert sind.
4. Typecheck sauber (Objektliteral korrekt, keine Syntaxfehler).
5. git log --oneline -3 + git status → NICHTS committet/gepusht; Tag pre-cooper-listen-2026-06-23 existiert. Bestätige: "Keine Commits, kein Push, keine Schema-Änderung."

Rollback: git reset --hard pre-cooper-listen-2026-06-23.
