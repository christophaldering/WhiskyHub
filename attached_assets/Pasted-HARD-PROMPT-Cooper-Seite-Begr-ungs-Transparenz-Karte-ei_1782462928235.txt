HARD PROMPT — Cooper-Seite: Begrüßungs-/Transparenz-Karte (einmalig) + Download (TXT + gebrandete PDF)

KONTEXT: In client/src/labs/pages/LabsTasteCooper.tsx kommen (1) zwei Download-Knöpfe auf die bestehende Gedächtnis-Karte: "Als Text" (.txt via downloadBlob) und "Als PDF" (gebrandet via downloadCooperMemoryPdf aus HP4); (2) ganz oben eine EINMALIGE, verwerfbare Begrüßungs-/Transparenz-Karte (localStorage "labs_cooper_area_intro_seen"). Voraussetzung: HP4 (client/src/lib/cooperMemoryPdf.ts) ist eingespielt. Eine Datei, reines Frontend, kein Schema/keine API.

STEP 0 — DIAGNOSE (nichts ändern bis "go"):
Bestätige (read-only): client/src/lib/cooperMemoryPdf.ts existiert und exportiert downloadCooperMemoryPdf; @/lib/download exportiert downloadBlob. In LabsTasteCooper.tsx: Importzeile `import { participantApi, participantUpdateApi, pidHeaders } from "@/lib/api";`; Zeile `const memStale = !!memUpdatedAt && ...`; die Positionierungs-Karte `<div className="labs-card p-5">` mit `<p className="labs-serif"`; die Knopf-Reihe mit "Aktualisieren"/"Löschen". `participant` (aus useQuery) ist verfügbar. Melde kurz, dann WARTE auf "go".

STEP 1 — Importe ergänzen
Ersetze
import { participantApi, participantUpdateApi, pidHeaders } from "@/lib/api";
durch
import { participantApi, participantUpdateApi, pidHeaders } from "@/lib/api";
import { downloadCooperMemoryPdf } from "@/lib/cooperMemoryPdf";
import { downloadBlob } from "@/lib/download";

STEP 2 — State + Handler nach der memStale-Zeile einfügen
Ersetze
  const memStale = !!memUpdatedAt && Date.now() - new Date(memUpdatedAt).getTime() > 30 * 86400000;
durch
  const memStale = !!memUpdatedAt && Date.now() - new Date(memUpdatedAt).getTime() > 30 * 86400000;
  const [introSeen, setIntroSeen] = useState(() => { try { return localStorage.getItem("labs_cooper_area_intro_seen") === "1"; } catch { return false; } });
  const dismissIntro = () => { try { localStorage.setItem("labs_cooper_area_intro_seen", "1"); } catch {} setIntroSeen(true); };
  const downloadMemTxt = async () => {
    if (!memText) return;
    try { await downloadBlob(new Blob([memText], { type: "text/plain;charset=utf-8" }), "casksense-sensorisches-gedaechtnis.txt"); }
    catch { toast({ description: "Download fehlgeschlagen.", variant: "destructive" }); }
  };
  const downloadMemPdf = async () => {
    if (!memText) return;
    try { await downloadCooperMemoryPdf(memText, memUpdatedAt, (participant as any)?.name); }
    catch { toast({ description: "PDF konnte nicht erstellt werden.", variant: "destructive" }); }
  };

STEP 3 — Begrüßungs-/Transparenz-Karte oben einfügen (vor der Positionierungs-Karte)
Ersetze
      <div className="labs-card p-5">
        <p className="labs-serif"
durch
      {!introSeen && (
        <div className="labs-card p-5 flex flex-col gap-3" data-testid="cooper-area-intro">
          <p className="text-sm font-semibold" style={{ color: "var(--labs-text)" }}>Hallo, ich bin Cooper.</p>
          <p className="text-xs" style={{ color: "var(--labs-text-muted)", lineHeight: 1.6 }}>
            Aus deinen Verkostungen entsteht nach und nach ein Bild von dir — dein Sensorisches Gedächtnis. Es liegt nur in deinem Bereich. Du entscheidest, ob ich im Nachgang darauf zurückgreife, und kannst es jederzeit ansehen, herunterladen oder löschen.
          </p>
          <button onClick={dismissIntro} data-testid="cooper-area-intro-dismiss"
            style={{ alignSelf: "flex-start", minHeight: 40, padding: "0 16px", borderRadius: 10, cursor: "pointer", background: "var(--labs-gold, #D4A847)", color: "#0B0906", fontSize: 14, fontWeight: 600, border: "none" }}>
            Verstanden
          </button>
        </div>
      )}

      <div className="labs-card p-5">
        <p className="labs-serif"

STEP 4 — Download-Knöpfe nach der Aktualisieren/Löschen-Reihe einfügen
Ersetze
            Löschen
          </button>
        </div>
      </div>
durch
            Löschen
          </button>
        </div>
        {memText && (
          <div style={{ display: "flex", gap: 8 }}>
            <button onClick={downloadMemTxt} data-testid="cooper-memory-download-txt"
              style={{ flex: 1, minHeight: 40, borderRadius: 10, cursor: "pointer", background: "transparent", color: "var(--labs-text)", fontSize: 13, border: "1px solid var(--labs-border)" }}>
              Als Text
            </button>
            <button onClick={downloadMemPdf} data-testid="cooper-memory-download-pdf"
              style={{ flex: 1, minHeight: 40, borderRadius: 10, cursor: "pointer", background: "transparent", color: "var(--labs-text)", fontSize: 13, border: "1px solid var(--labs-border)" }}>
              Als PDF
            </button>
          </div>
        )}
      </div>

VERIFIKATION (zeig mir):
  - grep -c "cooper-area-intro\|cooper-memory-download-txt\|cooper-memory-download-pdf" client/src/labs/pages/LabsTasteCooper.tsx → 4 (intro-Karte + intro-dismiss + 2 Download-Knöpfe).
  - grep -n "downloadCooperMemoryPdf\|downloadBlob\|introSeen" client/src/labs/pages/LabsTasteCooper.tsx → Importe + State + Handler vorhanden.
  - Build/Typecheck grün.
  - Funktion: oben einmalige Begrüßungskarte (verwerfbar, kommt nach „Verstanden" nicht wieder); auf der Gedächtnis-Karte erscheinen „Als Text" + „Als PDF" nur bei vorhandenem Porträt; PDF ist gebrandet (Gold/Serifen).

NICHT TUN: KEIN commit/push, KEIN drizzle/Schema, NUR diese Datei. Bei nicht-exaktem Match: STOPP und frag.
Rollback: git reset --hard <letzter Checkpoint>
