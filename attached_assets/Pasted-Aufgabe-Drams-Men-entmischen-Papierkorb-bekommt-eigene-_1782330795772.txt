Aufgabe: Drams-"..."-Menü entmischen — Papierkorb bekommt eigene Überschrift "Verwalten", getrennt vom Export. NUR client/src/labs/pages/LabsTasteDrams.tsx.

SICHERHEITSREGELN: KEINE Commits, KEIN Push, KEIN drizzle-kit, KEINE Schema-Änderung.

git tag pre-drams-menu-split-2026-06-24 setzen. Dann zwei Edits:

=== EDIT 1 — i18n für Sprachprüfung verfügbar machen. Ersetze EXAKT:
export default function LabsTasteDrams() {
  const { t } = useTranslation();
durch:
export default function LabsTasteDrams() {
  const { t, i18n } = useTranslation();

=== EDIT 2 — "Verwalten"-Überschrift vor dem Papierkorb. Ersetze EXAKT:
                      <button
                        onClick={() => { setMoreMenuOpen(false); setViewState("trash"); }}
                        className="w-full text-left flex items-center gap-2"
                        style={{ fontSize: 13, padding: "8px 10px", borderRadius: 8, color: "var(--labs-text)", background: "transparent", border: "none", cursor: "pointer" }}
                        data-testid="more-open-trash"
                      >
                        <Trash2 className="w-3.5 h-3.5" /> {t("drams.trash")}
                      </button>
durch:
                      <div style={{ padding: "6px 10px 4px", fontSize: 11, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.05em", color: "var(--labs-text-muted)" }}>{i18n.language?.toLowerCase().startsWith("de") ? "Verwalten" : "Manage"}</div>
                      <button
                        onClick={() => { setMoreMenuOpen(false); setViewState("trash"); }}
                        className="w-full text-left flex items-center gap-2"
                        style={{ fontSize: 13, padding: "8px 10px", borderRadius: 8, color: "var(--labs-text)", background: "transparent", border: "none", cursor: "pointer" }}
                        data-testid="more-open-trash"
                      >
                        <Trash2 className="w-3.5 h-3.5" /> {t("drams.trash")}
                      </button>

Verifikation:
1. grep -c "const { t, i18n } = useTranslation" client/src/labs/pages/LabsTasteDrams.tsx   (>=1)
2. grep -c "startsWith(\"de\") ? \"Verwalten\" : \"Manage\"" client/src/labs/pages/LabsTasteDrams.tsx   (1)
3. Typecheck sauber.
4. git status → nichts committet/gepusht. Bestätige: "Keine Commits, kein Push, keine Schema-Änderung."

Rollback: git reset --hard pre-drams-menu-split-2026-06-24.
