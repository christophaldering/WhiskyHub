Aufgabe: Glimmer-Bühne wirklich randlos (Kopf-/Fußleiste weg) + Fass größer.

Problem: Das Vollbild-Overlay der verbundenen Voice-Phase (position:fixed, zIndex 200) deckt App-Header und Bottom-Nav NICHT, weil ein transformierter Vorfahr (transform: scale(...) auf einer Labs-Klasse) position:fixed an sich bindet statt an den Viewport. Fix: das Overlay per createPortal an document.body hängen (Theme-Variablen kommen via :root:has(.labs-shell), bleiben also erhalten — wie bei ModalPortal).

Datei: client/src/labs/components/rating/ImpressionCapture.tsx

WICHTIGE REGELN: KEINE Commits, KEIN Push, KEINE Schema-/Migrationsänderung. Reiner Frontend-Change.

Schritt 0: git tag pre-glimmer-portal-2026-06-23 setzen. Bestätige per grep, dass die drei ALT-Stellen unten genau einmal vorkommen. Dann auf mein OK warten.

Änderung 1 — Import ergänzen. Nach der Zeile:
import { useState, useRef, useEffect } from "react";
neue Zeile einfügen:
import { createPortal } from "react-dom";

Änderung 2 — Overlay-Öffnung in ein Portal wickeln.
ALT:
          ) : (
            <div style={{ position: "fixed", inset: 0, zIndex: 200, background: "var(--labs-bg)", display: "flex", flexDirection: "column", alignItems: "center", padding: "calc(env(safe-area-inset-top) + 24px) 20px calc(env(safe-area-inset-bottom) + 16px)", overflowY: "auto" }}>
NEU:
          ) : createPortal((
            <div style={{ position: "fixed", inset: 0, zIndex: 200, background: "var(--labs-bg)", display: "flex", flexDirection: "column", alignItems: "center", padding: "calc(env(safe-area-inset-top) + 24px) 20px calc(env(safe-area-inset-bottom) + 16px)", overflowY: "auto" }}>

Änderung 3 — Overlay-Abschluss schließen + Portal-Ziel.
ALT:
                {t("v2.voiceAiNote", "Coopers Stimme ist KI-generiert.")}
              </div>
            </div>
          )}
NEU:
                {t("v2.voiceAiNote", "Coopers Stimme ist KI-generiert.")}
              </div>
            </div>
          ), document.body)}

Änderung 4 — Fass um ~30 % größer.
ALT:
                <div ref={glowWrapRef} style={{ display: "flex" }}><CooperBarrel size={176} live /></div>
NEU:
                <div ref={glowWrapRef} style={{ display: "flex" }}><CooperBarrel size={230} live /></div>

Verifikation:
1. grep -n "createPortal" ImpressionCapture.tsx → Import + createPortal(( + ), document.body) vorhanden.
2. grep -n "size={230}" ImpressionCapture.tsx → 1 Treffer.
3. Typecheck sauber (kein unbalanciertes Klammern/JSX).
4. git log --oneline -3 + git status → NICHTS committet/gepusht; Tag pre-glimmer-portal-2026-06-23 existiert. Bestätige: "Keine Commits, kein Push, keine Schema-Änderung."

Rollback: git reset --hard pre-glimmer-portal-2026-06-23.
