Aufgabe: Herkunftszeile unter der Verkostungsnotiz — Eigentum betonen ("aus deinen eigenen Worten verdichtet").

SICHERHEITSREGELN: KEINE Commits, KEIN Push, KEIN drizzle-kit, KEINE Schema-Änderung. Nur client/src/labs/pages/LabsTasteDrams.tsx (+ optional i18n.ts EN-Key).

Schritt 0 — Diagnose (NUR LESEN, dann auf mein OK warten):
1. git tag pre-narrative-provenance-2026-06-24 setzen.
2. client/src/labs/pages/LabsTasteDrams.tsx: die Anzeige-Verzweigung in TastingNarrativeSection zeigen (die Zeile `{!editing ? (` mit dem `labs-serif`-Div, ~Z. 1593–1596).
3. client/src/lib/i18n.ts: prüfen, ob im EN-Block ein Key `downloadNote:` unter `drams` existiert (dann gibt es dort EN-Übersetzungen).

Plan (erst nach OK):

A) LabsTasteDrams.tsx — Herkunftszeile in der Nicht-Editier-Ansicht ergänzen. Ersetze EXAKT:
      {!editing ? (
        <div className="labs-serif" style={{ fontSize: 16, color: "var(--labs-text-secondary)", lineHeight: 1.65, whiteSpace: "pre-wrap" }}>{value}</div>
      ) : (
durch:
      {!editing ? (
        <>
          <div className="labs-serif" style={{ fontSize: 16, color: "var(--labs-text-secondary)", lineHeight: 1.65, whiteSpace: "pre-wrap" }}>{value}</div>
          <div style={{ marginTop: 8, fontSize: 11, fontStyle: "italic", color: "var(--labs-text-muted)" }}>{t("drams.narrativeProvenance", "Aus deinen eigenen Worten im Gespr\u00e4ch verdichtet.")}</div>
        </>
      ) : (

B) i18n.ts — NUR falls in Schritt 0 ein EN-Key `downloadNote` unter `drams` existiert: direkt daneben ergänzen:
          narrativeProvenance: "Condensed from your own words in conversation.",
(Falls kein EN-drams-Block: weglassen, der deutsche Inline-Default genügt.)

Verifikation (zeig grep-Ausgaben):
1. grep -n "narrativeProvenance\|aus deinen eigenen Worten\|Aus deinen eigenen Worten" client/src/labs/pages/LabsTasteDrams.tsx
2. (falls B) grep -n "narrativeProvenance" client/src/lib/i18n.ts
3. Typecheck sauber (Fragment korrekt geschlossen).
4. git log --oneline -3 + git status → NICHTS committet/gepusht; Tag pre-narrative-provenance-2026-06-24. Bestätige: "Keine Commits, kein Push, keine Schema-Änderung."

Rollback: git reset --hard pre-narrative-provenance-2026-06-24.
